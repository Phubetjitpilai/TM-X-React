# Flow ของ `send_command.py` (บน Raspberry Pi) — ฉบับเล่าเป็นคำพูด

> เขียน 7 ส.ค. 2569 · เป็น flow **เป้าหมาย** หลังรวมงานข้อ E + F ของ
> `PLAN_criteria_and_multigroup.md` เข้าด้วยกัน ไม่ใช่สิ่งที่โค้ดทำอยู่ตอนนี้
>
> จุดที่ต่างจากของเดิมชัดๆ: Pi ดึงค่าการวัดเองด้วย `GM` แล้วตัดสิน OK/NG
> ส่งให้ MCU ทันที · สลับโปรแกรมวัดกลางคันได้ · เลิกเดาเวลาด้วย `sleep()`

---

## ช่วงที่ 0 — ตอนสคริปต์เพิ่งเปิด (สถานะพัก)

สคริปต์เริ่มต้นด้วย `is_running = False` แปลว่ายังไม่มีรอบการวัดใดๆ ทำงานอยู่
สิ่งที่ทำงานตอนนี้มีสองอย่างเท่านั้น

อย่างแรกคือ **HTTP server** ที่เปิดรอฟัง `POST /command` อยู่ที่พอร์ต `AGENT_PORT`
รอให้ Backend ยิงคำสั่ง `start` หรือ `stop` เข้ามา

อย่างที่สองคือ **thread ของ heartbeat** ที่ยิง `POST /api/heartbeat` ไปหา Backend
ทุก `HEARTBEAT_INTERVAL` วินาที ตลอดเวลาไม่ว่าจะกำลังวัดอยู่หรือไม่ ตอนพักอยู่มันจะ
ส่งไปโดยไม่แนบ `session_id` ซึ่งเป็นการบอก Backend ว่า "Pi ยังมีชีวิตอยู่นะ แต่ยังไม่ได้
วัดอะไร" thread นี้เป็น daemon จึงตายไปพร้อมโปรเซสเองโดยไม่ต้องสั่งปิด

ยังไม่มีการต่อ TCP ไปหา TM-X ในช่วงนี้ เพราะ TM-X ยอมให้มีอุปกรณ์ควบคุมได้ทีละตัว
ถ้าเราไปจองสายค้างไว้ **จอสัมผัสหน้าเครื่องจะถูกล็อก** ทั้งที่ยังไม่ได้ใช้งานอะไรเลย

---

## ช่วงที่ 1 — Backend สั่ง Start

Backend ยิง `POST /command` เข้ามาพร้อมก้อน JSON ที่หน้าตาแบบนี้

```json
{
  "action": "start",
  "session_id": 42,
  "target_count": 6,
  "groups": [
    {"template_name": "021", "alpl": [400, 401, 402],
     "limits": {"x_lo": 5.009999, "x_hi": 5.030001,
                "y_lo": 3.389999, "y_hi": 3.410001, "offset_max": null}},
    {"template_name": "007", "alpl": [501, 502, 503],
     "limits": {"x_lo": 3.199999, "x_hi": 3.240001,
                "y_lo": 3.199999, "y_hi": 3.240001, "offset_max": 0.03}}
  ]
}
```

**ตรวจก่อนรับงาน ไม่ใช่รับไว้ก่อนแล้วค่อยพังกลางคัน** — ต้องเช็คให้ครบว่า
มี `groups` จริงและไม่ว่าง · จำนวน ALPL รวมทุกกลุ่มเท่ากับ `target_count` ·
ไม่มีกลุ่มไหนที่ `alpl` ว่างเปล่า (ไม่งั้น `PW` จะถูกยิงฟรีโดยไม่มีชิ้นงานตามมา) ·
ไม่มี ALPL ซ้ำข้ามกลุ่ม ข้อไหนไม่ผ่านให้ตอบ error กลับไปทันทีแล้วจบ

> ที่ต้องเข้มตรงนี้เพราะของเดิมเคยปล่อยผ่าน — `target_count` ที่ไม่ได้ส่งมาเคยกลาย
> เป็น "วัด 1 ชิ้นแล้วจบ" อย่างเงียบๆ เพราะเขียน `(target_count or 1)` ไว้

ผ่านหมดแล้วจึงตั้ง `is_running = True` และ `current_session_id = session_id`
พอตั้งค่าตัวหลังปุ๊บ heartbeat จะเริ่มแนบ `session_id` ไปด้วยทุกครั้งทันที ซึ่งสำคัญมาก
เพราะถ้าไม่แนบ ตัว `heartbeat_checker` ฝั่ง Backend จะคิดว่า Agent ตายแล้ว
mark session เป็น `timeout` ภายใน ~15 วินาที แล้วหน้าเว็บของผู้ปฏิบัติงานจะถูกรีเซ็ตกลางคัน

จากนั้นแยก **thread ใหม่** ไปรันงานวัดจริง ส่วน endpoint ตอบ `{"status": "ok"}`
กลับไปหา Backend ทันที ห้ามทำงานวัดใน request นี้เด็ดขาด เพราะจะบล็อก HTTP server
ไว้ทั้งรอบ แล้วคำสั่ง `stop` ที่ตามมาทีหลังจะเข้าไม่ได้เลย

---

## ช่วงที่ 2 — เตรียม TM-X ให้พร้อมวัด

เปิด TCP ไปที่ TM-X หนึ่งสาย **แล้วใช้สายนี้สายเดียวตลอดทั้ง session** ทุกคำสั่ง
ทั้ง `RM`, `R0`, `PW`, `MRS`, `T1`, `GM`, `S0` วิ่งผ่านสายนี้หมด

> ⚠ ข้อนี้เปลี่ยนจากของเดิม โค้ดปัจจุบันเปิดสายที่สองเพื่อส่ง `T1` ทุกครั้ง ซึ่งทำให้
> **TM-X ตัดสายเดิมทิ้ง** (เพราะคุมได้ทีละอุปกรณ์) นี่คือสาเหตุที่ `S0` ตอนจบ session
> ไม่เคยสำเร็จเลยสักครั้ง — มันถูกยิงลงสายที่ตายไปแล้ว แล้ว `except: pass` ก็กลืน
> error ทิ้ง สุดท้าย TM-X ค้างอยู่ในโหมดวัด และจอขึ้น "Another device is currently in use"

**ขั้นแรกถามก่อนว่าตอนนี้อยู่โหมดไหน** ด้วยคำสั่ง `RM` ซึ่งจะตอบ `RM,0` (โหมดตั้งค่า)
หรือ `RM,1` (โหมดดำเนินการวัด) ถ้าได้ `1` อยู่แล้วก็ข้ามไปได้เลย ถ้าได้ `0` ให้ส่ง `R0`
เพื่อสลับเข้าโหมดวัด

**ขั้นที่สองคือรอให้โหมดเปลี่ยนจริง** ตรงนี้คือจุดที่พลาดกันมาตลอด — คู่มือหน้า 5-4
ระบุว่า `R0` เป็นคำสั่งที่ "เรียกใช้ข้อมูลคำสั่งทันที" หมายความว่ามันตอบกลับมาทันที
แต่การสลับโหมดจริงยังไม่เสร็จ ถ้ายิงคำสั่งถัดไปเลยจะได้ `ER,T1,03` ซึ่งรหัส `03` แปลว่า
"มีการออกคำสั่งในโหมดตั้งค่า" ไม่ได้เกี่ยวกับ connection อย่างที่เคยเข้าใจกันมา

วิธีที่ถูกคือ **วนถาม `RM` ซ้ำๆ ทุก ~0.3 วินาที จนกว่าจะได้ `1`** แล้วค่อยไปต่อ
ถ้าวนจนครบเวลาที่กำหนดแล้วยังไม่ได้ ให้ล้มเลิก session แล้วรายงาน ห้ามใช้
`time.sleep()` เดาเวลาเอาเหมือนของเดิม เพราะเดาผิดได้และอาการที่ออกมาไม่ได้ชี้ไปที่สาเหตุเลย

---

## ช่วงที่ 3 — จับมือกับ MCU

ก่อนเริ่มวัดต้องมั่นใจว่าฝั่ง MCU พร้อม — **รอให้ MCU รายงานว่าตัวเองอยู่สถานะ `idle`**
คือไม่มีชิ้นงานค้างอยู่ในระบบลำเลียงและเซนเซอร์พร้อมทำงาน

การรอตรงนี้ **ต้องมี timeout** ถ้า MCU ไม่ตอบภายในเวลาที่กำหนด ให้เลิก session
แล้วบอก Backend ห้ามรอแบบไม่มีที่สิ้นสุด เพราะถ้า MCU ไม่ได้เสียบสายอยู่ Pi จะค้าง
เงียบๆ โดยที่หน้าเว็บยังขึ้นว่า `running` และไม่มีใครรู้ว่าเกิดอะไรขึ้น

---

## ช่วงที่ 4 — ลูปนอก: ไล่ทีละกลุ่ม

วนตาม `groups` ที่ Backend ส่งมา หนึ่งรอบของลูปนี้คือหนึ่งกลุ่มชิ้นงานที่ใช้
โปรแกรมวัดและเกณฑ์ตัดสินชุดเดียวกัน

**เก็บ `limits` ของกลุ่มนี้ไว้ในตัวแปร** ตัวเลข 5 ตัวที่ Backend คำนวณมาให้แล้ว
(`x_lo`, `x_hi`, `y_lo`, `y_hi`, `offset_max`) เอาไว้ใช้ตัดสินทุกชิ้นในกลุ่มนี้
Pi ไม่ต้องรู้จัก `nominal` ไม่ต้องรู้จัก `tolerance` ไม่ต้องรู้ว่าเป็นโหมด IPM หรือ New
— แค่เอาค่าที่วัดได้มาเทียบว่าอยู่ในช่วงไหม

> เหตุผลที่ Backend คำนวณขอบเขตมาให้แทนที่จะส่ง `nominal`/`tolerance` ดิบ:
> เกณฑ์จริงมีค่าเผื่อ `_TOL_EPS = 1e-6` ซ่อนอยู่ (เพราะคอลัมน์เป็น `FLOAT`
> ค่า `5.02` อ่านกลับจาก DB ได้ `5.0199999809265137`) ถ้า Pi คำนวณเองแล้วลืมค่านี้
> จะตัดสินไม่ตรงกับ Backend **เฉพาะชิ้นที่ตกขอบพอดี** ซึ่งเป็นชิ้นที่สำคัญที่สุด
> และหาสาเหตุยากที่สุด

**สั่ง `PW,1,<template>` เพื่อโหลดโปรแกรมวัดของกลุ่มนี้** เลขโปรแกรมต้อง zero-pad
เป็น 3 หลักเสมอ (`"20"` ต้องกลายเป็น `"020"`) และ **ต้องตั้ง timeout ให้ยาวกว่าคำสั่งอื่นมาก**
ประมาณ 20 วินาที เพราะวัดจริงแล้วโปรแกรม 021 ใช้ 1.58 วิ ส่วน 020 ใช้ 3.72 วิ ซึ่งเฉียด
`settimeout(5.0)` เดิมมาก และยังมีโปรแกรมอื่นที่ยังไม่เคยจับเวลา

> พิสูจน์แล้วว่า `PW` ช้าเพราะ**ตัวโปรแกรมเอง** ไม่ใช่สะสมตามจำนวนชิ้นที่วัดไปแล้ว
> (ทดลองโหลด 021 ซ้ำหลังวัดไป 2 ชิ้น ได้ 1.54 วิ เท่ากับรอบแรก) แปลว่าวัด 50 ชิ้น
> ก็ไม่บานปลาย และการหน่วงเวลาก่อนยิง `PW` ไม่ช่วยอะไรเลย ต้องเพิ่ม timeout อย่างเดียว
> ซึ่งไม่มีข้อเสีย เพราะ timeout เป็นเพดาน ไม่ใช่การหน่วง — ตอบเร็วก็ไม่ได้รอนาน

หลัง `PW` ให้ **ยืนยันด้วย `RM` อีกครั้งว่ายังอยู่ในโหมดวัด** เพราะ `PW` พ่วง RESET
มาด้วย (คู่มือหน้า 5-79) ซึ่งล้างภาพประวัติและบัฟเฟอร์เอาต์พุต แล้วทำให้สัญญาณ READY
ดับไปชั่วขณะ

---

## ช่วงที่ 5 — ลูปใน: ไล่ทีละชิ้นในกลุ่ม

วนตาม `alpl` ของกลุ่มนี้ หนึ่งรอบคือหนึ่งชิ้นงาน แต่ละรอบมี 8 ขั้นตอน

### 5.1 เช็คก่อนว่ายังต้องวัดอยู่ไหม

ดู `is_running` ทุกครั้งก่อนเริ่มชิ้นใหม่ ถ้ากลายเป็น `False` แล้ว (มีคนกด Stop)
ให้ออกจากลูปทั้งสองชั้นทันที

### 5.2 รอสัญญาณ "ชิ้นงานพร้อมวัด" จาก MCU

MCU จะส่งสัญญาณมาเมื่อชิ้นงานถูกวางเข้าที่ใต้กล้องเรียบร้อยแล้ว การรอตรงนี้กินเวลา
ไม่แน่นอน อาจเป็นวินาทีหรือเป็นนาที จึงต้องรอแบบที่ **ยกเลิกได้** — วนเช็ค `is_running`
ทุก 0.1 วินาทีระหว่างรอ ไม่ใช่บล็อกค้างไว้เฉยๆ

> ของเดิมใช้ `input()` ซึ่งบล็อก thread ไว้ไม่มีกำหนดและปลุกจากข้างนอกไม่ได้
> กด Stop บนเว็บแล้ว TM-X หยุดทันทีจริง หน้าเว็บอัปเดตทันทีจริง แต่ thread ของ Pi
> ยังค้างอยู่ที่ `input()` → บล็อก `finally` ไม่ให้ทำงาน → socket ไม่ถูกปิด สถานะไม่ถูก
> เคลียร์ จนกว่าจะมีคนเดินไปเคาะ Enter ที่เทอร์มินัล ตอนนี้เปลี่ยนเป็น `threading.Event`
> แล้วซึ่งรับสัญญาณได้จากหลายแหล่ง (MCU ผ่าน Serial หรือ endpoint `/trigger`
> ที่เก็บไว้เป็น manual override เผื่อเซนเซอร์เสีย)

**ต้องใช้ Event คนละตัวกับสัญญาณ "จัดการเสร็จแล้ว" ในขั้น 5.7** ถ้าใช้ตัวเดียวกัน
สัญญาณ "จัดการเสร็จ" ของชิ้นก่อนที่มาช้าจะถูกนับเป็น "พร้อมวัด" ของชิ้นนี้ แล้ว `T1`
จะถูกยิงตอนที่ยังไม่มีชิ้นงานอยู่ใต้กล้อง

### 5.3 ล้างค่าเก่าใน TM-X ด้วย `MRS`

**ขั้นนี้สำคัญที่สุดในทั้ง flow และเป็นขั้นที่มองข้ามง่ายที่สุด**

`GM` ดึง "ค่าของภาพล่าสุด" และ**ไม่มีเลขลำดับกำกับมาด้วย** จึงมองไม่ออกเลยว่าค่าที่ได้
เป็นของชิ้นที่เพิ่งวัดหรือของชิ้นก่อนหน้า ถ้า `T1` รอบนี้วัดไม่ติด `GM` จะคืนค่าของชิ้นก่อน
มาให้เฉยๆ โดยไม่มี error ไม่มีอะไรเตือน แล้ว Pi จะตัดสินชิ้นใหม่ด้วยตัวเลขของชิ้นเก่า
**แล้วสั่ง MCU ให้ขยับของจริงตามนั้น**

ข้อมูลหน้างานวันที่ 31/07 คือ TM-X วัดไม่ติด **7 ครั้งจาก 8 ครั้ง** — ไม่ใช่กรณีหายาก

`MRS` ล้างค่าที่วัดได้ของโปรแกรมปัจจุบันทิ้ง ทำให้ระหว่างที่รอ `GM` **ต้อง** ตอบว่า
"ไม่มีค่า" เท่านั้น พอได้ค่าจริงเมื่อไหร่จึงมั่นใจได้ว่าเป็นของชิ้นใหม่แน่นอน

> ⚠ คู่มือหน้า 5-9 พิมพ์ชื่อคำสั่งไม่ตรงกันเอง หัวข้อเขียน `MRS` แต่ช่องส่ง/รับเขียน `MSR`
> ต้องทดสอบว่าตัวไหนใช้ได้จริง — `Test_T1_GM.py` ลองให้ทั้งสองแบบแล้ว

### 5.4 จำ `measured_count` ปัจจุบันไว้

อ่าน `GET /api/session/state` เก็บ `measured_count` ไว้เป็น `count_before`

**ต้องอ่านตรงนี้ ไม่ใช่ก่อนขั้น 5.2** เพราะช่วงรอสัญญาณอาจกินเวลาเป็นนาที ถ้าอ่านไว้
ตั้งแต่ก่อนรอ แล้วระหว่างนั้นมีค่าของ "ชิ้นก่อนหน้าที่มาช้า" หลุดเข้ามาผ่าน FTP
`measured_count` จะขยับตั้งแต่ยังไม่ได้ยิง `T1` ของชิ้นนี้ แล้วขั้น 5.8 จะเห็นว่า
"ขยับแล้ว" ทันทีและรายงานว่าชิ้นนี้สำเร็จ ทั้งที่ TM-X ยังไม่ได้วัดเลย

### 5.5 ยิง `T1` สั่งวัด

ส่งผ่านสายหลัก ถ้าได้ `ER,...,03` กลับมา แปลว่า READY ยังไม่กลับมาหลัง RESET
ที่พ่วงมากับ `PW` **ให้ยิงซ้ำได้อย่างปลอดภัย** เพราะรหัส `03` หมายถึงทริกเกอร์
*ถูกละเว้น* ไม่ได้วัดเลย จึงไม่มีทางได้ measurement ซ้ำสองอัน

### 5.6 วนถาม `GM` จนได้ค่าใหม่

`T1` ตอบกลับตอน **รับทริกเกอร์** ไม่ใช่ตอนวัดเสร็จ — คู่มือระบุใต้หัวข้อ `T1` ว่า
"เวลาในการประมวลผลการวัดจะไม่ได้รับผลกระทบ" แปลว่ามันคืนค่าทันทีโดยไม่รอผลวัด
ยิง `GM` ตามติดจึงยังไม่มีค่าให้ดึง

ให้วน `GM,3,0` ทุก ~20 มิลลิวินาที จนกว่าจะได้ค่าจริง (ไม่ใช่ `9999.999` และไม่ใช่
`ER,GM,03`) ภายในเวลา `GM_MAX_WAIT` — ค่านี้ได้จากการรัน `Test_T1_GM.py`

`GM,3,0` คืนมาในรูป `GM,t,m,i,j,…` โดย `m` คือค่าที่วัดได้ `i` คือสถานะ
(`1` = ค่าปกติ) และ `j` คือผลตัดสินของ TM-X เอง (`0` = OK, `1` = NG) จะได้ 3 ชุด
คือ `value_x`, `value_y` และ `offset`

**ถ้าครบเวลาแล้วยังไม่ได้ค่า** แปลว่า TM-X วัดชิ้นนี้ไม่ติดจริง ต้องบอก MCU ว่า
**"ไม่รู้ผล"** ซึ่งเป็นสถานะที่สามที่ต้องมี ไม่ใช่แค่ OK กับ NG แล้วให้ MCU ตัดสินใจว่า
จะจัดการกับชิ้นที่ไม่รู้ผลยังไง จากนั้นข้ามไปชิ้นถัดไป

### 5.7 ตัดสิน OK/NG แล้วบอก MCU

เอาค่าที่ได้มาเทียบกับ `limits` ที่เก็บไว้ตั้งแต่ลูปนอก

- `value_x` ต้องอยู่ระหว่าง `x_lo` ถึง `x_hi`
- `value_y` ต้องอยู่ระหว่าง `y_lo` ถึง `y_hi`
- ถ้า `offset_max` ไม่ใช่ `null` ค่าสัมบูรณ์ของ `offset` ต้องไม่เกิน `offset_max`
  (ถ้าเป็น `null` แปลว่าโหมดนี้ไม่ตรวจข้อนี้ ให้ถือว่าผ่าน)

ผ่านครบทุกข้อ = OK ตกข้อใดข้อหนึ่ง = NG แล้วส่งผลไปให้ MCU จัดการคัดแยกทันที

**เก็บ `j` ที่ TM-X ตัดสินมาเองไว้เทียบด้วย** ถ้าไม่ตรงกับที่เราคำนวณ แปลว่า tolerance
ที่ตั้งไว้ในโปรแกรมวัดของ TM-X กับที่อยู่ใน DB เพี้ยนกันแล้ว ให้ log ไว้เฉยๆ ไม่ต้องหยุด
สายการผลิต — ได้ตัวเฝ้าระวัง config drift มาฟรีๆ ซึ่งตอนนี้ระบบยังไม่มีเลย

จากนั้น **รอสัญญาณจาก MCU ว่าจัดการชิ้นนี้เสร็จแล้ว** ก่อนไปต่อ ไม่งั้นชิ้นถัดไปอาจถูก
วางเข้ามาทับตอนที่ชิ้นเดิมยังไม่ออกจากพื้นที่

### 5.8 ตรวจย้อนหลังว่าค่าลง DB หรือยัง

อ่าน `measured_count` อีกครั้ง วนดูว่ามากกว่า `count_before` ไหมภายในเวลาที่กำหนด

ค่านี้ยังมาทาง FTP เหมือนเดิม (TM-X ส่งไฟล์ไปที่ PC → `Recieve_tm-x.py` POST เข้า
Backend) — `GM` ที่เราเพิ่งใช้เป็นแค่ทางลัดสำหรับตัดสินให้ MCU เท่านั้น ไม่ได้แทนที่
เส้นทางบันทึกข้อมูล

> **ความหมายของขั้นนี้เปลี่ยนไปจากเดิม** ของเดิมมันคือ *ประตู* — ไม่ผ่านแปลว่ายังวัด
> ชิ้นนี้ไม่สำเร็จ จึงถามผู้ใช้ว่าจะวัดซ้ำไหม แต่ตอนนี้พอมาถึงขั้นนี้ **ของถูกคัดไปแล้ว
> และ MCU จัดการเสร็จแล้วด้วยซ้ำ** จะย้อนกลับไปวัดซ้ำไม่ได้อีก
>
> มันจึงกลายเป็น *การตรวจสอบย้อนหลัง* ว่าข้อมูลลง DB ครบไหม ถ้าไม่ขยับควรแค่
> **เตือนแล้ววัดต่อ** ไม่ใช่ถามว่าจะวัดซ้ำไหม เพราะคำถามนั้นไม่มีความหมายแล้ว
>
> ผลที่ยอมรับได้: ของถูกคัดถูกต้องแล้วแต่ไม่มีบันทึกใน DB — **ข้อมูลขาด ดีกว่าข้อมูลผิด**

เสร็จแล้ววนไปชิ้นถัดไปใน `alpl` ของกลุ่มนี้ พอหมดกลุ่มก็กลับขึ้นไปลูปนอกเพื่อ
`PW` โปรแกรมของกลุ่มถัดไป

---

## ช่วงที่ 6 — ออกจากลูป

ออกได้ 3 ทาง

**วัดครบทุกชิ้นทุกกลุ่ม** — ลูปจบเองตามธรรมชาติ

**มีคนกด Stop** — handler ของ `POST /command` ที่ `action: "stop"` ตั้ง
`is_running = False` แล้วยิง `S0` ไป TM-X ทันทีถ้า socket ยังใช้ได้ ส่วนลูปจะหลุด
ออกที่จุดเช็คถัดไป (ขั้น 5.1 หรือระหว่างรอสัญญาณในขั้น 5.2)

**เกิด exception** — จับไว้ log แล้วปล่อยให้ลง `finally`

ไม่ว่าจะออกทางไหน **`finally` ต้องทำงานเสมอ**

---

## ช่วงที่ 7 — `finally`: เก็บกวาดให้เรียบร้อย

ลำดับในนี้สำคัญ ทำผิดลำดับแล้วจะค้าง

**หนึ่ง — ตั้ง `_tmx_sock = None` ก่อนอย่างอื่น** เพราะขั้นตอนต่อไปจะเรียก
`POST /api/session/stop` ไปหา Backend ซึ่ง Backend จะยิง `POST /command`
`{"action":"stop"}` **กลับมาหา Pi** แล้ว handler ตัวนั้นจะพยายามส่ง `S0` ถ้า socket
ยังไม่เป็น `None` ถ้าเคลียร์ไว้ก่อน มันจะกลายเป็น no-op ที่ปลอดภัย

**สอง — ส่ง `S0` ให้ TM-X กลับเข้าโหมดตั้งค่า** แล้ว **log ผลจริงๆ ว่าได้อะไรกลับมา**
ห้าม `except: pass` กลืน error เหมือนของเดิม เพราะนั่นคือเหตุผลที่ไม่มีใครรู้มาตลอดว่า
`S0` ไม่เคยสำเร็จ และ `S0` มีลักษณะพิเศษคือ **ต่อคิวรองานที่ค้างอยู่** (คู่มือ: "หากกำลัง
สั่งงานเครื่องมืออยู่ จะมีการเรียกใช้คำสั่งหลังจากที่การสั่งงานเครื่องมือเสร็จสมบูรณ์")
ต่างจาก `R0` ที่ทำทันที จึงต้องเผื่อ timeout ให้ยาวกว่าปกติ

**สาม — ปิด socket ให้สะอาด** ด้วย `shutdown(SHUT_RDWR)` ก่อน `close()`
เพราะ `close()` เฉยๆ แค่คืน handle ให้ OS ไม่ได้ส่ง FIN บอก TM-X ทันทีเสมอไป
ทำให้ TM-X ยังคิดว่ามีอุปกรณ์ควบคุมเชื่อมอยู่ **แล้วจอสัมผัสหน้าเครื่องจะถูกล็อกต่อไป**
จนกว่าจะมีคนไปกด [Close Other Device] เอง

**สี่ — บอก MCU ว่ารอบการวัดจบแล้ว** MCU จะสั่งเซนเซอร์ให้หยุดทำงานแล้วกลับสู่
สถานะ `idle` จากนั้นรายงานกลับมาที่ Pi **การรอคำตอบนี้ต้องมี timeout เหมือนกัน**
ถ้า MCU ไม่ตอบก็ให้ log แล้วเดินหน้าต่อ ห้ามค้างรอ เพราะขั้นตอนที่ห้ายังไม่ได้ทำ

**ห้า — เคลียร์สถานะฝั่ง Pi** ตั้ง `current_session_id = None` เพื่อให้ heartbeat
กลับไปยิงแบบไม่แนบ session และตั้ง `is_running = False`

**หก — ปิด session ที่ Backend ถ้ายังค้างอยู่** อ่าน `GET /api/session/state`
ถ้า `session_id` ตรงกับตัวที่เราเพิ่งวัดและ `state` ยังเป็น `running` อยู่ ให้ยิง
`POST /api/session/stop`

> ทำไมเช็คแค่ `state` ไม่เทียบ `measured_count` กับ `target_count`: เพราะถ้าครบแล้ว
> Backend จะ auto-complete ปิด session ให้เองอยู่แล้ว การที่มาถึงตรงนี้แล้วยัง `running`
> อยู่จึงแปลว่าไม่ครบแน่นอน ไม่ต้องเทียบซ้ำ
>
> ทำไมต้องเทียบ `session_id` ด้วย: เพราะ `/api/session/state` คืน session ล่าสุดเสมอ
> (`ORDER BY session_id DESC LIMIT 1`) ถ้ามีคนกด Start รอบใหม่ไปแล้วระหว่างที่เรา
> กำลังเก็บกวาดอยู่ เราจะไปสั่งปิด session ของเขาแทน

ถ้ายิงไม่สำเร็จก็ไม่เป็นไร ให้ log ไว้ว่า Backend จะปิดเองผ่าน `heartbeat_checker`
ภายใน ~15 วินาที เพียงแต่ session จะขึ้นเป็น `timeout` แทนที่จะเป็น `stopped`

---

## ภาคผนวก ก — สัญญาณระหว่าง Pi กับ MCU

flow นี้ต้องการช่องทางสื่อสาร 5 แบบ ซึ่ง **ยังไม่มีในระบบเลย** ต้องออกแบบโปรโตคอลกันก่อน

| ทิศทาง | สัญญาณ | ใช้ที่ |
|---|---|---|
| MCU → Pi | ฉันอยู่สถานะ idle พร้อมทำงาน | ช่วง 3 |
| MCU → Pi | ชิ้นงานพร้อมวัดแล้ว | ขั้น 5.2 |
| Pi → MCU | ผลตัดสิน: OK / NG / **ไม่รู้ผล** | ขั้น 5.7 |
| MCU → Pi | จัดการชิ้นนี้เสร็จแล้ว | ขั้น 5.7 |
| Pi → MCU | รอบการวัดจบแล้ว | ช่วง 7 |

สองสัญญาณจาก MCU ที่เป็น "พร้อมวัด" กับ "จัดการเสร็จ" **ต้องแยก Event กัน**
ตามที่อธิบายในขั้น 5.2

---

## ภาคผนวก ข — เรื่องที่ยังไม่ได้ตัดสินใจ

**กด Stop ตอน MCU กำลังจัดการชิ้นอยู่ ควรทำยังไง** — รอให้ชิ้นนั้นจบก่อนแล้วค่อยหยุด
หรือตัดทันที ถ้าตัดทันทีแล้วบอก MCU ว่า "จบแล้ว" ทั้งที่มันยังทำงานค้างอยู่ จะเกิดอะไรขึ้น
กับชิ้นงานที่ค้างในระบบลำเลียง

**ชิ้นที่ "ไม่รู้ผล" ควรถูกนับเป็นชิ้นที่วัดแล้วไหม** — ถ้านับ `measured_count` จะไม่ตรง
กับจำนวนแถวใน `measurements` ถ้าไม่นับ session จะไม่มีทางถึง `target_count` แล้วต้อง
กด Stop เองทุกครั้งที่มีชิ้นวัดไม่ติด

**MCU ควรได้รู้เลข ALPL ไหม** — ตอนนี้ flow ส่งแค่ OK/NG ให้ ถ้าอยากให้ MCU
log หรือแสดงผลรายชิ้นได้ ต้องส่ง `alpl` ไปด้วย ซึ่ง Pi มีอยู่แล้วจาก `groups`
