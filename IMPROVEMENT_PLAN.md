# แผนปรับปรุง TM-X Pipeline

> ตรวจสอบกับโค้ดจริงเมื่อ 6 ส.ค. 2569 — `Recieve_tm-x.py` · `send_command(Pi).py` · `main.py` · `init.sql`

---

## ข้อสรุปหลัก

ระบบ **ไม่ได้ออกแบบผิด** — ทุกกรณีที่พังจบลงที่ "ไม่บันทึก" ซึ่งเป็น fail-safe ที่ถูกต้องสำหรับงานวัด

**ช่องโหว่จริงมีอย่างเดียว: ระบบรู้ว่าเกิดอะไรขึ้น แต่ไม่มีช่องทางบอกใคร**

- `Recieve_tm-x.py` รู้สาเหตุตั้งแต่วินาทีแรกเกือบทุกกรณี
- แต่ `print` ลง console บน PC ที่ไม่มีใครดู และไม่มีไฟล์เก็บ (`print` 39 ครั้ง · `logging` 0 ครั้ง)
- Pi ที่ต้องตัดสินใจ กลับต้องเดาจากความเงียบ 15 วินาที

---

## ⚠️ สถานะไฟล์ปัจจุบัน (ต่างจาก CLAUDE.md)

| ไฟล์ | ตำแหน่งจริงตอนนี้ |
|---|---|
| `Recieve_tm-x.py` | `Backend-server/` (ย้ายมาจาก `Backend-pc_station/`) |
| `send_command(Pi).py` | `Backend-pc_station/` (เปลี่ยนชื่อ มีวงเล็บ) |

ควรอัปเดต `CLAUDE.md` ให้ตรง

---

## กรณีที่ทำให้ `measured_count` ไม่เพิ่ม

| # | สาเหตุ | มี log | retry ช่วย |
|---|---|---|---|
| 1 | TM-X ไม่ได้รับ trigger / ค้าง / ไม่มีชิ้นงาน | ไม่มี | ไม่ |
| 2 | FTP ส่งไม่สำเร็จ | pyftpdlib | ไม่ |
| 3 | TM-X ส่งรูป HEAD-A แต่ไม่ส่ง `.txt` | ไม่มี | ไม่ |
| 4 | หาบรรทัดที่ `ts_key` ตรงไม่เจอใน 5 วิ | มี | ใช่ |
| 5 | ชื่อไฟล์แกะเวลาไม่ได้ + ไม่มี `.txt` | มี | ไม่ |
| 6 | `-9999.999` วัดไม่ติด | มี | อาจช่วย |
| 7 | ไม่มี session running | มี | — (ไม่มีใครรอ) |
| 8 | Backend ล่มตอน `GET /api/session/state` | มี | ไม่ |
| 9 | `POST` throw exception | มี | ขึ้นกับกรณี |
| 10 | Backend ตอบ 400/404/409/500 | มี | ไม่ |
| ~~11~~ | ~~ตอบ 200 แต่ rollback~~ | — | **ตัดออก** (ดูด้านล่าง) |
| 12 | TM-X เปลี่ยนชื่อโฟลเดอร์หัววัด | ไม่มีเลย | ไม่ |

**10 จาก 11 กรณีไหลมารวมที่จุดเดียว**: Pi รอ 15 วิ → ถาม User
→ ปรับที่จุดนั้นจุดเดียว ได้ผลกับ 10 กรณีพร้อมกัน

### ข้อยกเว้น

- **#7** เกิดนอก session ไม่มีใครรอ — ถูกต้องแล้ว
- **#9 ทาง B** Backend บันทึกสำเร็จแต่ response ไม่ถึง → `count` เพิ่มปกติ Pi เดินต่อ แต่**รูปไม่เคยถูกอัปโหลด ผ่านไปเงียบๆ**

---

## ผลการตรวจสอบกับโค้ดจริง

### ✅ ยืนยันว่าถูก

| ข้ออ้าง | หลักฐาน |
|---|---|
| `client_uuid` สุ่มใหม่ทุกครั้ง | `Recieve_tm-x.py:283` — `str(uuid.uuid4())` |
| `TXT_WAIT_TIMEOUT = 5.0` | บรรทัด 116 |
| Pi timeout 15 วิ | `.env` — `MEASURE_TIMEOUT=15` |
| ไม่มี retry ใน `POST` | ไม่มีคำว่า retry ทั้งไฟล์ |
| ใช้ `print` ไม่ใช่ `logging` | `print` 39 · `logging` 0 |
| รูปนอก HEAD-A โดนลบเงียบ | บรรทัด 548-553 `_remove_quietly()` ไม่ log |
| `measured_count` เพิ่มหลัง INSERT | `main.py:2239` อยู่หลัง `2217` |

### ❌ 4 จุดที่ต้องแก้ในแผน

**1. Unique index มีอยู่แล้ว — dedup พร้อมใช้ 100%**

```sql
client_uuid  VARCHAR(36)  NULL UNIQUE      -- init.sql:138
```

`main.py` ทำ dedup ครบ 2 ชั้นแล้ว:
- บรรทัด 2094-2111 เช็คก่อน insert → เจอซ้ำคืนผลเดิม ไม่นับ `measured_count` ซ้ำ
- บรรทัด 2226 จับ `IntegrityError` เป็นชั้นสำรอง

**ขาดอย่างเดียวคือค่าที่ใส่เป็นการสุ่ม → แก้บรรทัดเดียว ไม่ต้องแตะ DB**

**2. ไม่ต้องใช้ `uuid5`**

Column เป็น `VARCHAR(36)` ไม่ใช่ UUID type — `ts_key` ยาว 13 ตัวอักษร (`260731_172842`) ใส่ตรงๆ ได้เลย

**3. `_find_measurement_for_image` คืน 3 ช่องอยู่แล้ว**

```python
return parsed[0], parsed[1], parsed[2]      # value_x, value_y, offset
```

ไม่ต้องแก้ให้คืน 4 ช่อง เพราะจุดที่เรียก (บรรทัด 420) มี `ts_key` ในมืออยู่แล้ว

> ⚠️ ยกเว้นเส้นทาง `_fallback_last_line()` (ชื่อไฟล์แกะเวลาไม่ได้) ที่ไม่มี `ts_key` → ต้อง fallback เป็น `uuid4()` เหมือนเดิม

**4. ลำดับที่ TM-X ส่งของกลับกัน**

คอมเมนต์ในโค้ดบรรทัด 106-108 ยืนยันจากล็อกจริง:

```
HEAD-A → .txt → capture-image
```

**รูปมาก่อน `.txt` เสมอ** ซึ่งเป็นเหตุผลที่ต้องมี `TXT_WAIT_TIMEOUT` ตั้งแต่แรก
→ กรณีที่ 3 ในตารางต้องกลับด้านเป็น "ส่งรูปแต่ไม่ส่ง `.txt`"

### ตัดกรณี #11 ออก

`autocommit=True` ทำให้ `INSERT` กับ `UPDATE measured_count` เป็นคนละ transaction มีช่องว่างจริง **แต่กว้างแค่ 0.2-1 มิลลิวินาที**

วัดวันละ 500 ชิ้น = ช่วงเสี่ยงรวม 0.5 วินาที/วัน → โอกาสเกิด ≈ 0.001% (หลายสิบปีครั้ง)

ทิศทางความเสี่ยงก็ปลอดภัย: พลาดได้แค่ "เตือนเกินจริง" ไม่ใช่ "ข้อมูลหายเงียบ"

---

## คำถามยืนยัน

### ตอบได้แล้วจากโค้ด

| # | คำถาม | คำตอบ |
|---|---|---|
| 3 | เลข 10 หลักคืออะไร | รูปแบบชื่อไฟล์คือ `260731_172842_0000000008_capture-image_OK.bmp` หน้าตาเหมือนเลขรันของ TM-X — **ถ้ายืนยันว่าไม่รีเซ็ต จะเป็น dedup key ที่ดีกว่า `ts_key`** เพราะกันเคสวัด 2 ชิ้นในวินาทีเดียวได้ |
| 4 | มี unique index ไหม | **มี** + `main.py` ทำ dedup ครบแล้ว |
| 5 | `measured_count` เพิ่มตอนไหน | หลัง `INSERT` สำเร็จเสมอ · #11 ตัดออกได้ |

### ยังต้องยืนยันหน้างาน

| # | คำถาม | กระทบอะไร |
|---|---|---|
| 1 | ตอนวัดไม่ติด TM-X ส่งรูปมาด้วยไหม | `_is_error_value` ถูกเรียกที่บรรทัด 437, 516 ซึ่ง**อยู่ในเส้นทาง "หลังได้รูปมาแล้ว" ทั้งคู่** — ถ้าไม่ส่งรูป โค้ดตรงนั้นจะไม่เคยทำงานเลย แผนเรื่อง #6 ต้องคิดใหม่ทั้งหมด |
| 2 | Pi สั่ง trigger ใหม่ได้เองโดยไม่ต้องให้คนแตะชิ้นงานไหม | ถ้าต้องให้คนขยับก่อน → auto-retry ไร้ประโยชน์ ควรถาม User ตั้งแต่ครั้งแรก |
| 6 | `-9999` เกิดบ่อยแค่ไหน | เดือนละครั้ง → แค่ทำ log ให้ชัดพอ · ทุก 10 ชิ้น → ต้องแก้จริงจัง |
| 7 | 2 วิที่วัดมา นับจากไหน | ถ้าจากบรรทัด `⏱` = ไม่รวม FTP เวลาจริงน่าจะ 3-5 วิ · เอาไปตั้ง `MEASURE_TIMEOUT` กับ `TXT_WAIT_TIMEOUT` |
| ~~8~~ | ~~`GM,3,0` คืนอะไรมาจริง~~ | ✅ **ตอบแล้ว 7 ส.ค. 2569 — ดูหัวข้อถัดไป** |
| 8b | **index ไหนของ `GM` คือ x/y/offset** | ยังไม่รู้ — วิธีเร็วสุดคือเปิดโปรแกรม 021 ดูรายการเครื่องมือที่จอ TM-X · หรือขยับชิ้นงานทีละแกนแล้วดูว่าตัวไหนเปลี่ยน |
| 9 | เลข 10 หลักรีเซ็ตตอน `PW` ไหม | กระทบ `client_uuid` (ข้อ 4) — ดูจากชื่อไฟล์ที่กองอยู่ตอน `FORWARD_TO_BACKEND=0` |

---

## 🔬 ผลทดสอบ `Test_T1_GM.py` — 7 ส.ค. 2569 (โปรแกรม 021 · 5 ชิ้น)

### ✅ ยืนยันแล้ว เอาไปใช้ได้เลย

| เรื่อง | ผล |
|---|---|
| คำสั่งล้างค่า | **`MRS`** ใช้ได้ (ไม่ใช่ `MSR`) — ตอบ `'MRS'` ใน 3 ms |
| เวลาหลัง `T1` จนดึงค่าได้ | **147 · 153 · 148 · 267 · 255 ms** → กลาง 153 · ช้าสุด 267 |
| **`GM_MAX_WAIT`** | **ตั้ง 2.0 วิ** (ช้าสุด × ~7 เผื่อไว้เหลือเฟือ) |
| `PW,1,021` | **1494 ms** → ยืนยันซ้ำว่าต้องเพิ่ม timeout จาก `settimeout(5.0)` |
| `i` (สถานะ) | ใช้ได้จริง — `1` ค่าปกติ · `3` ข้อมูลไม่ถูกต้อง · `4` ล้างแล้วรอวัด |

### 📋 รูปแบบ response จริง

```
GM,0,+0005.065,1,0,+0005.076,1,0,+0001.979,1,0,+0002.141,1,0,
     +0016.969,1,0,+0020.701,1,0,-9999.999,3,0,-9999.999,3,0
```

**`t` ตอบกลับมาเป็น `0` ไม่ใช่จำนวนจริง** — ต้องนับจากความยาว body เอา (`len(body) // 3`)

```
[0] +5.065   [1] +5.076        ← ต่างกัน 0.011   น่าจะเป็นคู่ x/y
[2] +1.979   [3] +2.141        ← ต่างกัน 0.162
[4] +16.969  [5] +20.701       ← ต่างกัน 3.732
[6] ไม่มีค่า  [7] ไม่มีค่า  i=3   ← ไม่เคยมีค่าเลยทั้ง 5 ชิ้น
```

**8 เครื่องมือ ไม่ใช่ 3 อย่างที่คาด** — และ `.txt` ที่ระบบใช้อยู่มีแค่ 3 ช่อง
(`+0005.047,+0005.045,+0000.003` = x, y, offset)

### ⚠ 2 เรื่องที่ไม่เป็นอย่างที่ออกแบบไว้

**① ยังจับคู่ index กับ x/y/offset ไม่ได้**

`[0]`/`[1]` อยู่ราว 5.07 เหมือนคู่ x/y ใน `.txt` (5.047 / 5.045) แต่ **ไม่มีตัวไหนใกล้
`offset` = 0.003 เลย** — เป็นไปได้ว่า `offset` เป็นค่าที่ TM-X คำนวณตอนเขียนไฟล์
ไม่ใช่เครื่องมือใน `GM`

> ถ้าจริง → **Pi ดึง `offset` จาก `GM` ไม่ได้ → ตัดสินครบ 3 ข้อเองไม่ได้**
> กระทบแผนข้อ F โดยตรง ต้องหาคำตอบข้อ 8b ก่อนเริ่ม

**② `j` (ผลตัดสินของ TM-X) ใช้ไม่ได้ — เป็น `0` (OK) ทุกตัวทุกชิ้น**

รวมถึงเครื่องมือที่ `i=3` ไม่มีข้อมูลเลยก็ยังบอก OK แปลว่า **โปรแกรม 021 ไม่ได้ตั้ง
tolerance ไว้ในตัว TM-X** ทุกอย่างจึงผ่านโดยปริยาย

> **ตัดแผน "เอา `j` มาจับ config drift" ทิ้ง** — ใช้ไม่ได้จนกว่าจะไปตั้ง tolerance
> ที่หน้าเครื่อง ซึ่งเป็นงานคนละเรื่อง

### เกร็ดที่เจอเพิ่ม

- **หลัง `MRS` ได้ `-9999.999` + `i=4`** ไม่ใช่ `ER,GM,03` — มีแค่ครั้งแรกสุด
  (ยังไม่เคยวัดเลยตั้งแต่เปิดเครื่อง) ที่ได้ `ER,03` · ทั้งคู่แปลว่า "ไม่มีค่า" เหมือนกัน
- **`PW` ตอบแค่ `'PW'`** ไม่ได้ echo พารามิเตอร์กลับมา (ไม่ใช่ `'PW,1,021'`)
- TM-X ค้างอยู่ในโหมด Run จากรอบก่อน → `R0` + `RM` ผ่านใน 2 ms · ยืนยันว่าการไม่ส่ง
  `S0` ตอนจบทำให้รอบถัดไปเริ่มเร็วขึ้นจริง

---

## 💡 เครื่องมือที่มีอยู่แล้วแต่ยังไม่ได้ใช้

`Recieve_tm-x.py` มีโหมด `FORWARD_TO_BACKEND` อยู่แล้ว:

```
0 = รับไฟล์มากองไว้เฉยๆ ไม่ยิงเข้า Backend และไม่ลบไฟล์ทิ้ง
1 = โหมดใช้งานจริง   ← .env ตั้งค่านี้อยู่
```

**โหมด 0 คือเครื่องมือเก็บข้อมูลที่ต้องการพอดี** — เปิดแล้ววัดสัก 20 ชิ้น จะได้ครบในรอบเดียว:

- ชื่อไฟล์จริงทุกใบ + เลข 10 หลักเดินต่อเนื่องไหม (คำถาม #3)
- ตอนวัดไม่ติดมีรูปมาด้วยไหม (คำถาม #1)
- `STOR ... seconds=` จาก pyftpdlib เอาไปตั้ง timeout (คำถาม #7)

ไม่ต้องเขียนโค้ดเพิ่มเลย

---

## ลำดับการทำ

| ลำดับ | งาน | ขนาด | หมายเหตุ |
|---|---|---|---|
| 1 | **เปิด `FORWARD_TO_BACKEND=0` เก็บข้อมูล 20 ชิ้น** | 0 บรรทัด | ตอบคำถาม #1 #3 #7 พร้อมกัน |
| 2 | `logging` + `RotatingFileHandler` | กลาง | คุ้มสุด · ได้ประวัติย้อนหลังทันที |
| 3 | เตือนเมื่อเจอรูปนอก HEAD-A | เล็ก | กันเสียทั้ง lot (แก้ #12) |
| 4 | `client_uuid = ts_key + เลข 10 หลัก` | **1 บรรทัด** | ไม่ต้องแตะ DB · ดูด้านล่าง |
| 5 | `TXT_WAIT_TIMEOUT` 5 → 2 | เล็ก | ต้องมีตัวเลขจากข้อ 1 ก่อน |
| 6 | Pi timeout + retry + เมนู 3 ตัวเลือก | ใหญ่ | แก้หลายฝั่ง |
| 7 | `last_event` ใน `sessions` | กลาง | เติม column ไม่ต้องเพิ่ม endpoint |
| 8 | ปิด session ด้วย `position` + รายงาน `failed`/`pending` | กลาง | ตกลงกัน 7 ส.ค. — ดูด้านล่าง |
| 9 | `/command` เพิ่ม `continue` + ตัดการ poll ของ Pi | กลาง | ตกลงกัน 7 ส.ค. |
| 10 | modal นับถอยหลัง 60 วิที่หน้าเว็บ | เล็ก | ข้อ 9 |
| 11 | heartbeat นับ fail + รูปอัปโหลดพัง | เล็ก | ทำแยกได้ |
| 12 | โปรโตคอล Pi ↔ MCU + ตัด `S0` | กลาง | ต่อ MCU จริงได้ก่อน |

### รายละเอียดข้อ 4 — `client_uuid` (อัปเดต 7 ส.ค. 2569)

ชื่อไฟล์จาก TM-X มี **2 ส่วนที่ใช้เป็น key ได้**

```
260731_172842_0000000008_capture-image_OK.bmp
└── ts_key ──┘└─10 หลัก─┘

client_uuid = "260731_172842_0000000008"      24 ตัวอักษร (VARCHAR(36) รับได้)
```

**ใช้ทั้งคู่ ไม่ใช่อย่างใดอย่างเดียว**

| ใช้เดี่ยว | ชนเมื่อไหร่ |
|---|---|
| `ts_key` | วัด 2 ชิ้นในวินาทีเดียวกัน — `GM` วัดได้ ~350 ms/ชิ้น ถ้าต่อ MCU อัตโนมัติแล้วเกิดได้จริง |
| เลข 10 หลัก | **`PW` พ่วง RESET ที่ "สร้างไฟล์เอาต์พุตขึ้นใหม่" (คู่มือหน้า 5-79)** ตัวนับอาจกลับไปเริ่มที่ 1 |

ข้อหลังสำคัญขึ้นมากหลังทำแผนข้อ E — **เราสลับ `PW` กลางคันทุกกลุ่ม** ถ้าตัวนับรีเซ็ตทุกครั้ง
จะซ้ำกันในวันเดียวด้วยซ้ำ

รวมกันแล้วต้องซ้ำทั้ง**วินาที**และ**เลขนับ**พร้อมกันจึงจะชน — ไม่มีทางเกิด
และทั้ง 2 ส่วนแกะจาก**ชื่อไฟล์** จึงได้ค่าเดิมเสมอถ้าเป็นไฟล์เดิม (POST ซ้ำแล้วจับซ้ำได้)

> เส้นทาง `_fallback_last_line()` (ชื่อไฟล์แกะเวลาไม่ได้) ยังต้องใช้ `uuid4()` เหมือนเดิม
>
> 🔬 ยังต้องยืนยันหน้างานว่า **เลข 10 หลักรีเซ็ตตอน `PW` ไหม** — เปิด `FORWARD_TO_BACKEND=0`
> แล้ววัดคร่อมการสลับโปรแกรม ดูจากชื่อไฟล์ที่กองอยู่ ไม่ต้องเขียนโค้ดเพิ่ม

### รายละเอียดข้อ 3 — รูปนอก HEAD-A

ปัจจุบัน `parent != "head-a"` → `_remove_quietly()` **เงียบสนิท**

ถ้า TM-X ถูกเปลี่ยน config (HEAD-A → HEAD-B) รูปทุกใบจะโดนลบทิ้งเงียบๆ เสียทั้ง lot โดยไม่มี log แม้แต่บรรทัดเดียว

**ควรเป็น**: นับไว้ + เตือนเมื่อสะสมเกิน N ใบโดยไม่มี HEAD-A มาเลย

### รายละเอียดข้อ 6 — Pi timeout

**กฎที่ห้ามละเมิด: Pi timeout > worst case ของ happy path**

ถ้าตั้ง 4 วิ จะชนกับ `TXT_WAIT_TIMEOUT = 5.0` โดยตรง:

```
t=4.0  Pi ยอมแพ้ → retry ยิง trigger ใหม่
t=4.5  .txt มาถึง → เธรดเดิม POST สำเร็จ
t=4.8  TM-X วัดรอบใหม่ → อีก 1 measurement
       → count เพิ่ม 2 สำหรับชิ้นงานชิ้นเดียว
```

`client_uuid` กันไม่ได้ เพราะสองรอบนั้นเป็นการวัดจริงคนละครั้ง `ts_key` ต่างกัน

```
Pi timeout = FTP + TXT_WAIT(2) + session query + POST + margin ≈ 6-8 วิ
```

**Retry ต้องมีเพดาน** — ไม่งั้น #12 จะกลายเป็น "ยิง trigger ทุก 6 วิไม่หยุด" ตลอดทั้งกะ

```
retry ครั้งที่ 1 → รอ timeout
retry ครั้งที่ 2 → รอ timeout
ยังไม่ได้ → หยุด ถาม User
```

**เมนูถาม User เปลี่ยนเป็น 3 ตัวเลือก**

```
วัดไม่ติด 2 ครั้งติด (ชิ้นที่ 4/10)
  [1] ลองใหม่อีกครั้ง      ← ควรมีที่สุด แต่ตอนนี้ไม่มี
  [2] ข้ามชิ้นนี้
  [3] จบการทำงาน
```

### รายละเอียดข้อ 7 — `last_event` (ออกแบบใหม่ 7 ส.ค. 2569)

#### แกนความคิด — **Pi รู้อาการ · Recieve รู้สาเหตุ · Backend เป็นคนรวม**

```
Pi      รายงาน  "ชิ้นที่ 3/6 ไม่ได้ค่าใน 8 วิ"     ← เมื่อไหร่ · ชิ้นไหน
Recieve รายงาน  "TM-X วัดไม่ติด (-9999.999)"      ← เพราะอะไร
─────────────────────────────────────────────────────────────
Backend รวม     "ALPL 402 (ชิ้นที่ 3/6) วัดไม่ติด (-9999.999)"
```

**ไม่มีใครต้องรู้จักกัน** — ต่างคนต่างรายงานสิ่งที่ตัวเองเห็น
**Pi ไม่อ่าน `last_event`** (ต่างจากที่เคยร่างไว้ก่อนหน้า)

#### 3 คอลัมน์ใหม่ใน `sessions`

```sql
last_event        VARCHAR(32) NULL,
last_event_detail TEXT        NULL,
last_event_at     DATETIME    NULL
```

#### `POST /api/session/event` — endpoint ใหม่ให้ Recieve ยิง

```json
{"event": "MEASURE_FAILED",
 "detail": "TM-X วัดไม่ติด (-9999.999, -9999.999)"}
```

```sql
UPDATE sessions SET last_event = %s, last_event_detail = %s, last_event_at = NOW()
 WHERE state = 'running';
```

**ไม่ต้องส่ง `session_id`** — Recieve ไม่รู้ในบางด่าน (ด่าน 1-2 มาก่อนด่าน 3 ที่เป็นตัวถาม session)

#### ⚠ ทั้ง 2 คอลัมน์มาจากคนเดียวกันเสมอ

| กรณี | ใครเขียน | `last_event` | `last_event_detail` |
|---|---|---|---|
| Recieve ทิ้งค่า | **Recieve** | `MEASURE_FAILED` · `TXT_NOT_FOUND` · `NO_SESSION` · `BACKEND_REJECT` | ข้อความจาก Recieve |
| Pi ไปต่อไม่ได้ | **Pi** (ผ่าน `/api/session/stop`) | `PI_ERROR` | `reason` จาก Pi |
| Pi timeout | **ไม่มีใคร** | — | — (Backend ไปอ่านของ Recieve) |
| รูปอัปโหลดพัง | **ไม่มีใคร** | — | — (ดูข้อ 11) |

#### Backend รวมตอน Pi รายงาน timeout

```python
fresh = last_event_at and (now - last_event_at).total_seconds() < 30
detail = last_event_detail if fresh else None       # ⚠ ต้องเช็คอายุ

await push_event("measure_timeout", {
    "session_id": ..., "piece": ..., "target": ...,
    "number_alpl": queue[position],     # ← Backend เติม (Pi ไม่ส่งมา)
    "detail":      detail,              # ← Backend เติมจาก last_event
})
```

**ไม่เช็คอายุจะเอาสาเหตุของชิ้นที่ 1 เมื่อ 3 นาทีก่อนมาแปะให้ชิ้นที่ 5**

#### กติกาของ payload — **`piece`/`target` ผ่านตรงๆ Backend เติมแค่ 2 ฟิลด์**

```json
Pi →       {"session_id": 42, "piece": 3, "target": 6}

Backend →  {"session_id": 42, "piece": 3, "target": 6,      ← ผ่านตรงๆ ห้ามแปลง
            "number_alpl": 402,                              ← เติม จาก queue[position]
            "detail": "TM-X วัดไม่ติด (-9999.999, ...)"}      ← เติม จาก last_event
```

**`number_alpl` ต้องมาจาก Backend ไม่ใช่จาก Pi** — ถ้า Pi ส่งมาเองจะมี 2 แหล่งที่บอกว่า
ชิ้นนี้คือ ALPL อะไร แล้วเหลื่อมกันได้ (Pi นับจาก `groups` · Backend ใช้ `queue[position]`)
ผู้ใช้จะเห็นเลขไม่ตรงกับที่ระบบบันทึกจริง

#### ⚠ `detail` ต้องเป็น `null` ได้ — หน้าเว็บต้องรองรับ

```
มี detail    →  "ALPL 402 (ชิ้นที่ 3/6) วัดไม่ติด (-9999.999, -9999.999)"
ไม่มี detail →  "ALPL 402 (ชิ้นที่ 3/6) ไม่ได้รับค่าภายในเวลาที่กำหนด"
```

เกิดเมื่อ — รูปไม่มาเลย (Recieve ไม่เคยรายงาน) · `last_event` เก่าเกิน 30 วิ · Recieve แจ้งไม่สำเร็จ

#### ⚠ `last_event` มีช่องเดียว — ค่าใหม่ทับค่าเก่า

```
t=3   ชิ้นที่ 3 วัดไม่ติด    → last_event = MEASURE_FAILED    ← Backend ต้องใช้ตัวนี้
t=8   รูปชิ้นที่ 2 อัปโหลดพัง → ถ้าเขียนทับ → Pi รายงานตอน t=11 แล้วได้ข้อความผิดเรื่อง
```

**กฎ: `last_event` เก็บเฉพาะสาเหตุที่ทำให้ค่าไม่ถูกบันทึกลง DB** เท่านั้น
เรื่องที่ค่าลงไปแล้ว (รูปพัง) ห้ามเขียนลงช่องนี้

#### ครอบคลุมไม่ครบ — ต้องออกแบบให้ `detail` เป็น `null` ได้

```
รูปไม่มาเลย  →  _handle_capture ไม่ถูกเรียก  →  Recieve เงียบสนิท
```

```
มี detail    →  "ALPL 402 วัดไม่ติด (-9999.999)"
ไม่มี detail →  "ALPL 402 ไม่ได้รับค่าภายใน 8 วินาที"
```

> ยังต้องยืนยันคำถาม #1 — **TM-X ส่งรูปมาด้วยไหมตอนวัดไม่ติด** ถ้าไม่ส่ง Recieve จับกรณีนี้ไม่ได้เลย

### รายละเอียดข้อ 8 — ปิด session ด้วย `position`

> ตกลงกัน 7 ส.ค. 2569

#### ปัญหา: `measured_count` ตอบผิดคำถาม

ตอนนี้ auto-complete เช็ค `measured_count >= target_count` (`main.py:2343`)
แต่ 2 ตัวนี้ตอบคนละคำถาม

```
measured_count  →  "วัดสำเร็จไปกี่ชิ้น"
position        →  "เดินไปถึงไหนของคิวแล้ว"
```

ปกติเท่ากัน **จะต่างกันเมื่อมีการข้ามชิ้น** (ผู้ใช้ตอบ `continue` ตอน TM-X วัดไม่ติด
— `resolve_measure_timeout` ขยับ `position` แต่ไม่ได้ insert measurement)

```
คิว 6 ชิ้น ข้ามชิ้นที่ 3
   measured_count = 5      ← ไม่มีวันถึง target ตลอดกาล
   position       = 6      ← หมดคิวแล้วจริงๆ
```

**"วัดครบ" ที่แท้จริงคือ "ไม่เหลืออะไรให้วัดแล้ว" = `position` ถึงปลายคิว**
ส่วน `measured_count == target` เป็นแค่เรื่องบังเอิญที่จริงตอนไม่มีการข้าม

#### ตอนนี้ยังไม่พัง — แต่พึ่ง Pi

```
position = 6 → auto-complete ไม่ทำงาน
             → Pi วนครบ 6 ชิ้นแล้วหลุดลูป → finally เห็น 'running'
             → POST /api/session/stop → 'stopped' บันทึก 5/6 ✅
```

จบถูกต้อง **แต่ถ้า Pi พังก่อนถึง `finally`** session จะค้างจนโดน mark `timeout`
แทนที่จะเป็น `stopped` → บันทึกสาเหตุผิด

การเปลี่ยนไปใช้ `position` = **ตัด coupling กับ Pi ออก** Backend ปิดเองได้

#### ⚠ ห้ามเอา `measured_count` ออก

**Pi ใช้มันเป็นตัวยืนยันว่าค่าลง DB จริงไหม** (`wait_for_measurement()`) และ
`position` ใช้แทนไม่ได้ เพราะ **การข้ามชิ้นก็ขยับ `position` เหมือนกัน**

```
Pi poll position       → เห็นขยับ → "ชิ้นนี้สำเร็จ!"   ← ผิด อาจเป็นการข้าม
Pi poll measured_count → ขยับเฉพาะตอน INSERT จริง ✅
```

หลังแก้ `measured_count` จะเหลือหน้าที่เดียว — ให้ Pi ยืนยัน (ยังจำเป็นแม้หลังทำ
ข้อ F ของ `PLAN_criteria_and_multigroup.md` ตาม `FLOW_send_command.md` ขั้น 5.8)

#### ต้นทุน: ต้องมีฟังก์ชันกลาง

`position` ขยับ **2 ที่** (`main.py:2316` และ `:1026`) ส่วน `measured_count` ขยับที่เดียว
ถ้าย้ายไปเช็ค `position` ต้องเอา auto-complete ไปใส่ทั้ง 2 ที่ ไม่งั้นเคส
**"ข้ามชิ้นสุดท้าย"** จะไม่ปิดตัวเอง

```python
def _complete_if_queue_done(cur, session_id: int, qstate: dict) -> bool:
    """ปิด session ถ้าเดินหมดคิวแล้ว — เรียกจากทุกที่ที่ขยับ position"""
    if qstate["position"] < len(qstate["queue"]):
        return False
    cur.execute("UPDATE sessions SET state='stopped', ended_at=NOW() "
                "WHERE session_id=%s", (session_id,))
    session_queues.pop(session_id, None)
    return True
```

> ⚠️ **ห้ามเช็คทั้ง 2 ตัว** — ต้องเลือกให้เหลือตัวเดียวเป็นเจ้าของคำว่า "จบ"
> มี 2 เจ้าของเมื่อไหร่จะเริ่มขัดกันเองแล้วหาสาเหตุยากกว่าเดิม

#### ของแถม: รู้ว่าพังตรงไหน โดยไม่ต้องเพิ่มคอลัมน์

ข้อมูลครบอยู่แล้ว แค่ยังไม่มีใครเอามาประกอบ

```
queue    = [400, 401, 402, 501, 502, 503]
position = 4                                  ← กด Stop ตอนนี้
measurements ของ session นี้ = [400, 401, 501]

index 0-3  (< position)   เคยพยายามแล้ว
   400 ✅  401 ✅  402 ❌ ไม่มีแถว = พลาด/ข้าม   501 ✅
index 4-5  (>= position)  ยังไม่เคยแตะเลย
   502 · 503 = ไม่ได้วัด
```

**นี่คือคุณค่าที่แท้จริงของ `position`** — มันแยก **"วัดแล้วพลาด"** ออกจาก
**"ยังไม่ได้วัด"** ซึ่ง `measured_count` ทำไม่ได้เลย (บอกได้แค่ "5 จาก 6"
โดยไม่รู้ว่าตัวไหน)

เพิ่มเป็น**ค่าคำนวณ**ใน `GET /api/session/state`

```json
{
  "session_id": 42, "state": "stopped",
  "queue":    [400, 401, 402, 501, 502, 503],
  "position": 4,
  "measured": 3,
  "failed":   [402],           ← index < position แต่ไม่มีแถวใน measurements
  "pending":  [502, 503]       ← index >= position
}
```

หน้าเว็บขึ้นได้ตรงๆ ว่า *"วัดได้ 3/6 · ALPL 402 วัดไม่ติด · 502, 503 ยังไม่ได้วัด"*

**เก็บลงคอลัมน์จะแย่กว่า** เพราะกลายเป็นข้อมูลชุดที่ 3 ที่ต้อง sync ให้ตรงกับอีก 2 ชุด
— ผิดเมื่อไหร่จะขัดกันเองแล้วไม่รู้ว่าเชื่อตัวไหน ส่วนแบบคำนวณ **ผิดไม่ได้เลยโดยธรรมชาติ**

#### สรุปที่จะแก้

| | เดิม | ใหม่ |
|---|---|---|
| ปิด session | `measured_count >= target_count` | `position >= len(queue)` ผ่าน `_complete_if_queue_done()` |
| `measured_count` | ใช้ปิด session + Pi ยืนยัน | **เหลือหน้าที่เดียว** — Pi ยืนยัน |
| รู้ว่าพังตรงไหน | ไม่รู้เลย | `failed` / `pending` คำนวณใน `/api/session/state` |

### รายละเอียดข้อ 9 — `/command` รับ 3 action · Pi เลิก poll

> ตกลงกัน 7 ส.ค. 2569

**คำสั่งจากข้างนอกเข้าทางเดียวหมด** — Pi ไม่ต้อง poll อะไรอีกเลย

```
เดิม  Pi ──POST /api/measure-timeout──▶ Backend ──SSE──▶ modal
      Pi ◀──GET .../{id} (วนทุก 0.4 วิ)── Backend ◀──POST .../resolve──┘

ใหม่  Pi ──POST /api/measure-timeout──▶ Backend ──SSE──▶ modal
      Pi ◀──POST /command {"continue"}── Backend ◀──POST /api/session/continue──┘
```

#### ฝั่ง Pi

```python
elif req.action == "continue":
    _continue.set()                     # threading.Event — pattern เดียวกับ _trigger
else:
    raise HTTPException(400, f"ไม่รู้จัก action '{req.action}'")
```

```python
def ask_user_on_timeout(session_id, piece, target):
    _continue.clear()                                   # ล้างสัญญาณค้างจากรอบก่อน
    httpx.post(f"{BACKEND_URL}/api/measure-timeout", json={...})
    while True:
        if not is_running:      return "stop"           # /command stop
        if _continue.wait(0.1): return "continue"       # /command continue
```

**ตื่นทันทีที่คำตอบมาถึง** ไม่ต้องรอรอบ poll ถัดไป

#### ฝั่ง Frontend — 3 ปุ่ม 3 endpoint ชื่อสมมาตรกัน

```
▶ Start        →  POST /api/session/start
■ Stop         →  POST /api/session/stop        ← ปุ่ม Stop · "หยุด" ใน modal · หมดเวลา 60 วิ
▶ วัดชิ้นถัดไป  →  POST /api/session/continue    {"session_id": 42}
```

**หน้าเว็บไม่เคยเรียก `/command`** — `/command` เป็นเส้น Backend → Pi เท่านั้น

#### ⚠ ลำดับใน `/api/session/continue` ห้ามสลับ

```python
qstate["position"] += 1                    # ① ขยับคิวก่อน
UPDATE sessions SET queue_state = ...      # ② sync ลง DB
POST /command {"action": "continue"}       # ③ ค่อยปลุก Pi
```

ปลุก Pi ก่อนขยับ `position` → **ชิ้นถัดไปถูกแปะ ALPL ผิด** (Pi วิ่งเร็วกว่า UPDATE ได้)

และต้องเช็คว่ามีคำถามค้างอยู่จริง (`measure_timeouts`) ไม่งั้นตอบ `404` — กันหน้าเว็บยิง
continue ตอนไม่มีใครถาม แล้ว `position` ขยับฟรี

#### ลบได้

```
❌ GET /api/measure-timeout/{session_id}
❌ measure_timeouts[sid]["decision"]
❌ การ poll ทุก 0.4 วิระหว่างรอคำตอบ
```

`measure_timeouts` ยังเก็บไว้เป็น **ยามเฝ้าประตู** ให้ `continue` ตอบ 404 ได้

### รายละเอียดข้อ 10 — modal นับถอยหลังที่หน้าเว็บ

**หน้าเว็บเป็นคนนับ ไม่ใช่ Pi** เพราะการวัดต้องมีคนวางชิ้นงานอยู่แล้ว ไม่มีคน = ไม่มี modal

```
⚠️ ALPL 402 (ชิ้นที่ 3/6) วัดไม่ติด (-9999.999, -9999.999)
   จะหยุดอัตโนมัติใน 43 วินาที
   [วัดชิ้นถัดไป]  [หยุดการวัด]
```

```javascript
let mtTimer = null;
function onMeasureTimeout(d) {
  showModal(...);
  let left = 60;
  mtTimer = setInterval(() => {
    setCountdownText(--left);
    if (left <= 0) { clearInterval(mtTimer); stopSession(); }
  }, 1000);
}
function closeMtModal() { clearInterval(mtTimer); mtTimer = null; ... }
```

**`clearInterval` ต้องเรียกทุกทางที่ modal ปิด** — กดวัดต่อ · กดหยุด · ได้ SSE
`session_stopped`/`session_timeout` ไม่งั้นนาฬิกาเดินต่อแล้วยิง stop ใส่ session ถัดไป

**modal ต้องปิดเองเมื่อได้ `session_stopped`/`session_timeout`** ไม่งั้นค้างบนจอแล้วกดตอบไปก็ไม่มีอะไรเกิดขึ้น

> ช่องที่เหลือ — แท็บถูกปิด/เบราว์เซอร์ค้างตอน modal โผล่ → Pi รอตลอดกาล
> ทางออก: เปิดเว็บใหม่แล้วกด Stop (`/command stop` ยังถึง Pi ปกติ)
> ถ้าอยากปิดช่องนี้ ใส่ backstop หยาบๆ ที่ Pi (เช่น 10 นาที) — ไม่ใช่ตัวหลัก แค่กันค้างข้ามคืน

### รายละเอียดข้อ 11 — heartbeat นับ fail · รูปอัปโหลดพัง

#### heartbeat — Pi หยุดเองเมื่อยิงไม่ออกนานพอ

```python
_hb_fail = 0
try:
    httpx.post(f"{BACKEND_URL}/api/heartbeat", json={...}, timeout=5)
    _hb_fail = 0
except Exception:
    _hb_fail += 1
    if _hb_fail * HB_INTERVAL > HB_TIMEOUT_HINT and is_running:
        log.warning("ยิง heartbeat ไม่ออก %d ครั้งติด — หยุดวัด", _hb_fail)
        is_running = False          # + บอก MCU ให้หยุดด้วย
```

**ใช้ค่าเดียวกับที่ Backend ใช้ตัดสิน** (`HEARTBEAT_TIMEOUT` = 15 วิ) Pi จึงหยุดตัวเองพอดี
ตอนที่ *"Backend ต้อง mark timeout ไปแล้วแน่ๆ"* — ไม่มีทางตัดสินขัดกัน

`HB_TIMEOUT_HINT` มีอยู่ในโค้ดแล้ว (`send_command(Pi).py:67`) แต่ยังใช้แค่พิมพ์ข้อความเตือน

> ❌ **ไม่ทำ `should_stop` ใน response ของ heartbeat** — บน LAN นิ่งๆ เหลืองานแค่เคสเดียว
> (เสียบสายกลับภายใน 20 วิ) ซึ่งการนับ fail ครอบคลุมอยู่แล้ว และต้องแก้ 2 ไฟล์แทนที่จะเป็น 1

#### รูปอัปโหลดพัง — toast + ดูจากอายุแถว

```python
except Exception as exc:
    log.error("อัปโหลดรูปไม่สำเร็จ (measurement_id=%s): %s", mid, exc)
    report("IMAGE_UPLOAD_FAILED", ..., persist=False)     # ← ไม่เขียน last_event
```

```javascript
station_event: (d) => showToast(`⚠ ${d.detail}`)          // เตือนแบบไม่ขวางทาง
```

**ห้ามให้ Pi อ่าน event นี้** — ค่าบันทึกลง DB สำเร็จไปแล้ว ขาดแค่รูป ไม่กระทบการวัดเลย

#### หน้าเว็บรู้ว่า "ไม่มีรูป" จากอายุแถว ไม่ต้องเพิ่มคอลัมน์

```javascript
function imgCell(m) {
  if (m.image_path) return imgBtn(m.measurement_id);
  const ageSec = (Date.now() - new Date(m.measure_time)) / 1000;
  return ageSec < 60 ? '' : '<span class="no-img">⚠ ไม่มีรูป</span>';
}
```

`image_path = null` แปลได้ 2 อย่าง (**ยังไม่มา** / **พังแล้ว**) — ใช้อายุแถวแยกออก
รูปปกติมาถึงใน ~1 วิ เกิน 60 วิแล้วยังไม่มา = ไม่มาแน่นอน

**จับได้ครอบคลุมกว่าการให้ Recieve แจ้ง** เพราะไม่ต้องพึ่งว่าจะมีใครมาแจ้ง (Recieve
ตายกลางทางก็ยังจับได้) และไม่ต้องเพิ่มคอลัมน์ · endpoint · การ sync

> tooltip เดิมใน `imgCell()` (`index.html:3005`) ยังเขียนว่า *"อัปโหลดขึ้น **MinIO** ไม่สำเร็จ
> หลังลอง **3 ครั้ง**"* — MinIO เลิกใช้แล้วและไม่มี retry ต้องแก้ข้อความด้วย

### รายละเอียดข้อ 12 — โปรโตคอล Pi ↔ MCU · ตัด `S0`

> ตกลงกัน 7 ส.ค. 2569

#### สัญญาณเหลือ 4 แบบ — **Pi ถามทุกครั้ง MCU ไม่เคยเริ่มพูดเอง**

| ทิศทาง | สัญญาณ | ใช้ตอน |
|---|---|---|
| Pi → MCU | ถามสถานะ | ตอนเริ่ม — วนถามจนได้ `IDLE` |
| Pi → MCU | **ถามว่าชิ้นงานพร้อมยัง** | ก่อนทุกชิ้น |
| Pi → MCU | ผลตัดสิน **OK / NG / ไม่รู้ผล** | หลังได้ค่าจาก `GM` |
| Pi → MCU | รอบการวัดจบแล้ว → กลับ `IDLE` | ตอน `finally` |

#### ⭐ รวม "จัดการเสร็จแล้ว" เข้ากับ "พร้อมยัง" — เหลือสัญญาณเดียว

แผนเดิมจะมี 2 สัญญาณแยก (*"ชิ้นงานพร้อม"* กับ *"จัดการชิ้นก่อนเสร็จ"*) ซึ่งเสี่ยงสับสนกัน
— สัญญาณ "จัดการเสร็จ" ที่มาช้าจะถูกนับเป็น "พร้อมวัด" ของชิ้นถัดไป แล้ว `T1` ยิงตอนยังไม่มีชิ้นงาน

**รวมเป็นตัวเดียวแล้วความกำกวมหายไปทั้งหมด**

```
❗ นิยามที่ต้องเขียนไว้ในสเปกให้ชัด
   "พร้อม" = จัดการชิ้นก่อนหน้าเสร็จแล้ว **และ** ชิ้นใหม่เข้าที่แล้ว
```

ถ้าคนเขียนฝั่ง MCU ตีความว่า *"พร้อม = มีชิ้นงานอยู่ใต้กล้อง"* เฉยๆ **Pi จะยิง `T1`
ตอนที่ MCU ยังคัดชิ้นก่อนไม่เสร็จ** ซึ่งไม่มีอะไรจับได้เลยจากฝั่ง Pi

#### และเป็นการ **ถาม** ไม่ใช่ **รอสัญญาณ**

```
push (รอสัญญาณ)  →  ต้อง clear() ก่อนรอ → มีช่องแคบที่สัญญาณมาพอดีตอน clear แล้วหาย
poll (ถาม)       →  ถามซ้ำได้เรื่อยๆ ตอบผิดจังหวะก็ถามใหม่ ไม่มีอะไรหาย
```

#### ⚠ ผลตัดสินต้องมี **3 ค่า ไม่ใช่ 2**

```
OK        วัดได้และผ่านสเปก
NG        วัดได้แต่ไม่ผ่านสเปก
ไม่รู้ผล    วัดไม่ได้ — GM ไม่คืนค่า / i = 2,3 / ครบ GM_MAX_WAIT
```

**ห้ามส่ง `NG` ตอนวัดไม่ได้** — `NG` แปลว่า "วัดแล้วไม่ผ่าน" ส่วนชิ้นที่วัดไม่ได้อาจดีก็ได้
คัดไปกอง NG จะทิ้งของดีทิ้งไปเปล่าๆ

#### ตัด `S0` ตอนจบ — แต่ยังต้องปิด socket ให้สะอาด

```python
finally:
    _tmx_sock = None                        # ต้องมาก่อนแจ้ง Backend เสมอ
    try: sock.shutdown(socket.SHUT_RDWR)    # ← ยังต้องมี แม้ไม่ส่ง S0
    except OSError: pass
    sock.close()
```

**TM-X ล็อกจอสัมผัสตราบใดที่ยังเห็นว่ามีอุปกรณ์ควบคุมเชื่อมอยู่** — `close()` เฉยๆ ไม่พอ
ต้อง `shutdown()` ส่ง FIN บอกตรงๆ

การค้างในโหมด Run ไม่มีปัญหา — คำสั่งที่ระบบใช้ทั้งหมด (`R0`/`PW`/`T1`/`RM`/`GM`/`MRS`)
ทำงานในโหมดวัดได้ และ `R0` สั่งได้จากทั้ง 2 โหมด รอบหน้าจึงเริ่มได้ปกติ **เร็วกว่าด้วย
เพราะไม่ต้องสลับโหมด** · ถ้าต้องแก้โปรแกรมหน้าเครื่องใช้ `send_s0.py` แยก

---

## สิ่งที่ไม่ควรทำ

- ❌ แยก error 12 กรณีละเอียด — 3 ประเภทพอ
- ❌ hash / เข้ารหัส `ts_key` เพื่อความสวยงาม — เสีย correlation key ฟรีๆ (ค่านี้ grep ทะลุได้ทั้งระบบ: ชื่อไฟล์รูป → บรรทัดใน `.txt` → log → แถวใน DB ด้วย string เดียวกัน) ถ้ากังวลว่าดูเหมือน column date ให้แก้ที่**ชื่อ field** เป็น `source_key` / `tmx_key`
- ❌ ใส่ `session_id` นำหน้า `client_uuid` — ไฟล์ที่มาถึงซ้ำอาจคร่อม session คนละอัน key จะต่างกัน ซ้ำหลุด
- ❌ retry ไม่จำกัดจำนวน
- ❌ ตั้ง Pi timeout สั้นกว่า `TXT_WAIT_TIMEOUT`
- ❌ **เอา `measured_count` ออกหลังย้ายไปปิด session ด้วย `position`** — Pi ยังต้องใช้
  ยืนยันว่าค่าลง DB จริง (ข้อ 8) · และ ❌ **เช็คทั้ง `position` และ `measured_count`
  พร้อมกัน** ต้องเหลือเจ้าของคำว่า "จบ" ตัวเดียว
- ❌ เก็บ `failed`/`pending` เป็นคอลัมน์ — คำนวณเอาจาก `queue` + `position` + `measurements`
- ❌ **เขียน `IMAGE_UPLOAD_FAILED` ลง `last_event`** — จะทับสาเหตุที่ Backend ต้องหยิบไปตอบ Pi
- ❌ **ให้ Pi อ่าน `last_event`** — Pi รายงานอาการ Backend เป็นคนรวมสาเหตุ
- ❌ **ให้ Pi ส่ง `number_alpl` มากับ `measure-timeout`** — จะมี 2 แหล่งที่บอกว่าชิ้นนี้คือ ALPL อะไร
  แล้วเหลื่อมกันได้ · ให้ Backend เติมจาก `queue[position]` แทน
- ❌ **ให้ทั้งหน้าเว็บและ Pi นับ 60 วิพร้อมกัน** — 2 นาฬิกาแข่งกัน วันหนึ่งตั้งไม่ตรงจะงงว่าใครหยุด
- ❌ **เพิ่มคอลัมน์ `image_upload_failed`** — ใช้อายุแถวแทน จับได้ครอบคลุมกว่าและไม่ต้อง sync
- ❌ รื้อ architecture — ปัญหาอยู่ที่การส่งข้อมูล ไม่ใช่โครงสร้าง

---

## ข้อจำกัดที่รับทราบไว้ (ไม่ต้องแก้ตอนนี้)

- **`ts_key` ละเอียดแค่ 1 วินาที** — วัดสองชิ้นในวินาทีเดียวจะจับคู่ชนกัน ในทางปฏิบัติงานนี้กดมือทีละชิ้น แทบเป็นไปไม่ได้ ถ้าอนาคตต่อ conveyor อัตโนมัติต้องเปลี่ยนไปใช้เลข 10 หลักแทน
- **บรรทัดใน `.txt` อาจถูกจับคู่ซ้ำ** — ไม่มีการ mark ว่าใช้ไปแล้ว
- **#9 ทาง B** — measurement มีใน DB แต่ไม่มีรูป ไม่มีใครรู้ ต้องแก้แยก
- **`_find_measurement_for_image` อ่านไฟล์ใหม่ทั้งไฟล์ทุก 0.3 วิ** — เปลืองแต่ไฟล์เล็ก ยังไม่เป็นปัญหาจริง
- **`measured_count` ไม่ลดตอนลบ measurement** — ลบผ่านถังขยะแล้ว counter ค้างสูงถาวร มีผลตอนทำรายงานย้อนหลัง
