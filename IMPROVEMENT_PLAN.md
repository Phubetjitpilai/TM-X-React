# แผนปรับปรุง TM-X Pipeline

> ตรวจสอบกับโค้ดจริงเมื่อ 6 ส.ค. 2569 — `Recieve_tm-x.py` · `send_command(Pi).py` · `main.py` · `init.sql`

---

## ข้อสรุปหลัก

ระบบ **ไม่ได้ออกแบบผิด** — ทุกกรณีที่พังจบลงที่ "ไม่บันทึก" ซึ่งเป็น fail-safe ที่ถูกต้องสำหรับงานวัด

**ช่องโหว่จริงมีอย่างเดียว: ระบบรู้ว่าเกิดอะไรขึ้น แต่ไม่มีช่องทางบอกใคร**

- `Recieve_tm-x.py` รู้สาเหตุตั้งแต่วินาทีแรกเกือบทุกกรณี
- แต่ `print` ลง console บน PC ที่ไม่มีใครดู และไม่มีไฟล์เก็บ (`print` 39 ครั้ง · `logging` 0 ครั้ง)
- Pi ที่ต้องตัดสินใจ กลับต้องเดาจากความเงียบ 15 วินาที

---

## ⚠️ สถานะไฟล์ปัจจุบัน (ต่างจาก CLAUDE.md)

| ไฟล์ | ตำแหน่งจริงตอนนี้ |
|---|---|
| `Recieve_tm-x.py` | `Backend-server/` (ย้ายมาจาก `Backend-pc_station/`) |
| `send_command(Pi).py` | `Backend-pc_station/` (เปลี่ยนชื่อ มีวงเล็บ) |

ควรอัปเดต `CLAUDE.md` ให้ตรง

---

## กรณีที่ทำให้ `measured_count` ไม่เพิ่ม

| # | สาเหตุ | มี log | retry ช่วย |
|---|---|---|---|
| 1 | TM-X ไม่ได้รับ trigger / ค้าง / ไม่มีชิ้นงาน | ไม่มี | ไม่ |
| 2 | FTP ส่งไม่สำเร็จ | pyftpdlib | ไม่ |
| 3 | TM-X ส่งรูป HEAD-A แต่ไม่ส่ง `.txt` | ไม่มี | ไม่ |
| 4 | หาบรรทัดที่ `ts_key` ตรงไม่เจอใน 5 วิ | มี | ใช่ |
| 5 | ชื่อไฟล์แกะเวลาไม่ได้ + ไม่มี `.txt` | มี | ไม่ |
| 6 | `-9999.999` วัดไม่ติด | มี | อาจช่วย |
| 7 | ไม่มี session running | มี | — (ไม่มีใครรอ) |
| 8 | Backend ล่มตอน `GET /api/session/state` | มี | ไม่ |
| 9 | `POST` throw exception | มี | ขึ้นกับกรณี |
| 10 | Backend ตอบ 400/404/409/500 | มี | ไม่ |
| ~~11~~ | ~~ตอบ 200 แต่ rollback~~ | — | **ตัดออก** (ดูด้านล่าง) |
| 12 | TM-X เปลี่ยนชื่อโฟลเดอร์หัววัด | ไม่มีเลย | ไม่ |

**10 จาก 11 กรณีไหลมารวมที่จุดเดียว**: Pi รอ 15 วิ → ถาม User
→ ปรับที่จุดนั้นจุดเดียว ได้ผลกับ 10 กรณีพร้อมกัน

### ข้อยกเว้น

- **#7** เกิดนอก session ไม่มีใครรอ — ถูกต้องแล้ว
- **#9 ทาง B** Backend บันทึกสำเร็จแต่ response ไม่ถึง → `count` เพิ่มปกติ Pi เดินต่อ แต่**รูปไม่เคยถูกอัปโหลด ผ่านไปเงียบๆ**

---

## ผลการตรวจสอบกับโค้ดจริง

### ✅ ยืนยันว่าถูก

| ข้ออ้าง | หลักฐาน |
|---|---|
| `client_uuid` สุ่มใหม่ทุกครั้ง | `Recieve_tm-x.py:283` — `str(uuid.uuid4())` |
| `TXT_WAIT_TIMEOUT = 5.0` | บรรทัด 116 |
| Pi timeout 15 วิ | `.env` — `MEASURE_TIMEOUT=15` |
| ไม่มี retry ใน `POST` | ไม่มีคำว่า retry ทั้งไฟล์ |
| ใช้ `print` ไม่ใช่ `logging` | `print` 39 · `logging` 0 |
| รูปนอก HEAD-A โดนลบเงียบ | บรรทัด 548-553 `_remove_quietly()` ไม่ log |
| `measured_count` เพิ่มหลัง INSERT | `main.py:2239` อยู่หลัง `2217` |

### ❌ 4 จุดที่ต้องแก้ในแผน

**1. Unique index มีอยู่แล้ว — dedup พร้อมใช้ 100%**

```sql
client_uuid  VARCHAR(36)  NULL UNIQUE      -- init.sql:138
```

`main.py` ทำ dedup ครบ 2 ชั้นแล้ว:
- บรรทัด 2094-2111 เช็คก่อน insert → เจอซ้ำคืนผลเดิม ไม่นับ `measured_count` ซ้ำ
- บรรทัด 2226 จับ `IntegrityError` เป็นชั้นสำรอง

**ขาดอย่างเดียวคือค่าที่ใส่เป็นการสุ่ม → แก้บรรทัดเดียว ไม่ต้องแตะ DB**

**2. ไม่ต้องใช้ `uuid5`**

Column เป็น `VARCHAR(36)` ไม่ใช่ UUID type — `ts_key` ยาว 13 ตัวอักษร (`260731_172842`) ใส่ตรงๆ ได้เลย

**3. `_find_measurement_for_image` คืน 3 ช่องอยู่แล้ว**

```python
return parsed[0], parsed[1], parsed[2]      # value_x, value_y, offset
```

ไม่ต้องแก้ให้คืน 4 ช่อง เพราะจุดที่เรียก (บรรทัด 420) มี `ts_key` ในมืออยู่แล้ว

> ⚠️ ยกเว้นเส้นทาง `_fallback_last_line()` (ชื่อไฟล์แกะเวลาไม่ได้) ที่ไม่มี `ts_key` → ต้อง fallback เป็น `uuid4()` เหมือนเดิม

**4. ลำดับที่ TM-X ส่งของกลับกัน**

คอมเมนต์ในโค้ดบรรทัด 106-108 ยืนยันจากล็อกจริง:

```
HEAD-A → .txt → capture-image
```

**รูปมาก่อน `.txt` เสมอ** ซึ่งเป็นเหตุผลที่ต้องมี `TXT_WAIT_TIMEOUT` ตั้งแต่แรก
→ กรณีที่ 3 ในตารางต้องกลับด้านเป็น "ส่งรูปแต่ไม่ส่ง `.txt`"

### ตัดกรณี #11 ออก

`autocommit=True` ทำให้ `INSERT` กับ `UPDATE measured_count` เป็นคนละ transaction มีช่องว่างจริง **แต่กว้างแค่ 0.2-1 มิลลิวินาที**

วัดวันละ 500 ชิ้น = ช่วงเสี่ยงรวม 0.5 วินาที/วัน → โอกาสเกิด ≈ 0.001% (หลายสิบปีครั้ง)

ทิศทางความเสี่ยงก็ปลอดภัย: พลาดได้แค่ "เตือนเกินจริง" ไม่ใช่ "ข้อมูลหายเงียบ"

---

## คำถามยืนยัน

### ตอบได้แล้วจากโค้ด

| # | คำถาม | คำตอบ |
|---|---|---|
| 3 | เลข 10 หลักคืออะไร | รูปแบบชื่อไฟล์คือ `260731_172842_0000000008_capture-image_OK.bmp` หน้าตาเหมือนเลขรันของ TM-X — **ถ้ายืนยันว่าไม่รีเซ็ต จะเป็น dedup key ที่ดีกว่า `ts_key`** เพราะกันเคสวัด 2 ชิ้นในวินาทีเดียวได้ |
| 4 | มี unique index ไหม | **มี** + `main.py` ทำ dedup ครบแล้ว |
| 5 | `measured_count` เพิ่มตอนไหน | หลัง `INSERT` สำเร็จเสมอ · #11 ตัดออกได้ |

### ยังต้องยืนยันหน้างาน

| # | คำถาม | กระทบอะไร |
|---|---|---|
| 1 | ตอนวัดไม่ติด TM-X ส่งรูปมาด้วยไหม | `_is_error_value` ถูกเรียกที่บรรทัด 437, 516 ซึ่ง**อยู่ในเส้นทาง "หลังได้รูปมาแล้ว" ทั้งคู่** — ถ้าไม่ส่งรูป โค้ดตรงนั้นจะไม่เคยทำงานเลย แผนเรื่อง #6 ต้องคิดใหม่ทั้งหมด |
| 2 | Pi สั่ง trigger ใหม่ได้เองโดยไม่ต้องให้คนแตะชิ้นงานไหม | ถ้าต้องให้คนขยับก่อน → auto-retry ไร้ประโยชน์ ควรถาม User ตั้งแต่ครั้งแรก |
| 6 | `-9999` เกิดบ่อยแค่ไหน | เดือนละครั้ง → แค่ทำ log ให้ชัดพอ · ทุก 10 ชิ้น → ต้องแก้จริงจัง |
| 7 | 2 วิที่วัดมา นับจากไหน | ถ้าจากบรรทัด `⏱` = ไม่รวม FTP เวลาจริงน่าจะ 3-5 วิ |

---

## 💡 เครื่องมือที่มีอยู่แล้วแต่ยังไม่ได้ใช้

`Recieve_tm-x.py` มีโหมด `FORWARD_TO_BACKEND` อยู่แล้ว:

```
0 = รับไฟล์มากองไว้เฉยๆ ไม่ยิงเข้า Backend และไม่ลบไฟล์ทิ้ง
1 = โหมดใช้งานจริง   ← .env ตั้งค่านี้อยู่
```

**โหมด 0 คือเครื่องมือเก็บข้อมูลที่ต้องการพอดี** — เปิดแล้ววัดสัก 20 ชิ้น จะได้ครบในรอบเดียว:

- ชื่อไฟล์จริงทุกใบ + เลข 10 หลักเดินต่อเนื่องไหม (คำถาม #3)
- ตอนวัดไม่ติดมีรูปมาด้วยไหม (คำถาม #1)
- `STOR ... seconds=` จาก pyftpdlib เอาไปตั้ง timeout (คำถาม #7)

ไม่ต้องเขียนโค้ดเพิ่มเลย

---

## ลำดับการทำ

| ลำดับ | งาน | ขนาด | หมายเหตุ |
|---|---|---|---|
| 1 | **เปิด `FORWARD_TO_BACKEND=0` เก็บข้อมูล 20 ชิ้น** | 0 บรรทัด | ตอบคำถาม #1 #3 #7 พร้อมกัน |
| 2 | `logging` + `RotatingFileHandler` | กลาง | คุ้มสุด · ได้ประวัติย้อนหลังทันที |
| 3 | เตือนเมื่อเจอรูปนอก HEAD-A | เล็ก | กันเสียทั้ง lot (แก้ #12) |
| 4 | `client_uuid = ts_key` | **1 บรรทัด** | ไม่ต้องแตะ DB |
| 5 | `TXT_WAIT_TIMEOUT` 5 → 2 | เล็ก | ต้องมีตัวเลขจากข้อ 1 ก่อน |
| 6 | Pi timeout + retry + เมนู 3 ตัวเลือก | ใหญ่ | แก้หลายฝั่ง |
| 7 | `last_event` ใน `sessions` | กลาง | เติม column ไม่ต้องเพิ่ม endpoint |

### รายละเอียดข้อ 3 — รูปนอก HEAD-A

ปัจจุบัน `parent != "head-a"` → `_remove_quietly()` **เงียบสนิท**

ถ้า TM-X ถูกเปลี่ยน config (HEAD-A → HEAD-B) รูปทุกใบจะโดนลบทิ้งเงียบๆ เสียทั้ง lot โดยไม่มี log แม้แต่บรรทัดเดียว

**ควรเป็น**: นับไว้ + เตือนเมื่อสะสมเกิน N ใบโดยไม่มี HEAD-A มาเลย

### รายละเอียดข้อ 6 — Pi timeout

**กฎที่ห้ามละเมิด: Pi timeout > worst case ของ happy path**

ถ้าตั้ง 4 วิ จะชนกับ `TXT_WAIT_TIMEOUT = 5.0` โดยตรง:

```
t=4.0  Pi ยอมแพ้ → retry ยิง trigger ใหม่
t=4.5  .txt มาถึง → เธรดเดิม POST สำเร็จ
t=4.8  TM-X วัดรอบใหม่ → อีก 1 measurement
       → count เพิ่ม 2 สำหรับชิ้นงานชิ้นเดียว
```

`client_uuid` กันไม่ได้ เพราะสองรอบนั้นเป็นการวัดจริงคนละครั้ง `ts_key` ต่างกัน

```
Pi timeout = FTP + TXT_WAIT(2) + session query + POST + margin ≈ 6-8 วิ
```

**Retry ต้องมีเพดาน** — ไม่งั้น #12 จะกลายเป็น "ยิง trigger ทุก 6 วิไม่หยุด" ตลอดทั้งกะ

```
retry ครั้งที่ 1 → รอ timeout
retry ครั้งที่ 2 → รอ timeout
ยังไม่ได้ → หยุด ถาม User
```

**เมนูถาม User เปลี่ยนเป็น 3 ตัวเลือก**

```
วัดไม่ติด 2 ครั้งติด (ชิ้นที่ 4/10)
  [1] ลองใหม่อีกครั้ง      ← ควรมีที่สุด แต่ตอนนี้ไม่มี
  [2] ข้ามชิ้นนี้
  [3] จบการทำงาน
```

### รายละเอียดข้อ 7 — `last_event`

เติม column ใน query ที่ Pi ทำอยู่แล้ว ไม่ต้องเพิ่ม endpoint ไม่ต้องเพิ่มการ poll

```sql
-- เดิม
SELECT measured_count FROM sessions WHERE session_id = ?
-- ใหม่
SELECT measured_count, last_event, last_event_at FROM sessions WHERE session_id = ?
```

แยกแค่ 3 ประเภทพอ ไม่ต้องแยก 12 กรณี:

| ค่า | ครอบกรณี | Pi ทำอะไร |
|---|---|---|
| `MEASURE_FAILED` | #6 | ถาม "วัดไม่ติด ลองใหม่ไหม" |
| `SYSTEM_ERROR` | #8 #10 #12 | หยุด แจ้งว่าระบบมีปัญหา |
| (ว่าง) | #1 #2 #3 #4 #5 | timeout ตามเดิม |

**กลุ่มที่ 3 เท่านั้นที่ต้องพึ่ง timeout จริงๆ** อีกสองกลุ่ม `Recieve_tm-x.py` รู้คำตอบตั้งแต่วินาทีแรกแล้ว

---

## สิ่งที่ไม่ควรทำ

- ❌ แยก error 12 กรณีละเอียด — 3 ประเภทพอ
- ❌ hash / เข้ารหัส `ts_key` เพื่อความสวยงาม — เสีย correlation key ฟรีๆ (ค่านี้ grep ทะลุได้ทั้งระบบ: ชื่อไฟล์รูป → บรรทัดใน `.txt` → log → แถวใน DB ด้วย string เดียวกัน) ถ้ากังวลว่าดูเหมือน column date ให้แก้ที่**ชื่อ field** เป็น `source_key` / `tmx_key`
- ❌ ใส่ `session_id` นำหน้า `client_uuid` — ไฟล์ที่มาถึงซ้ำอาจคร่อม session คนละอัน key จะต่างกัน ซ้ำหลุด
- ❌ retry ไม่จำกัดจำนวน
- ❌ ตั้ง Pi timeout สั้นกว่า `TXT_WAIT_TIMEOUT`
- ❌ รื้อ architecture — ปัญหาอยู่ที่การส่งข้อมูล ไม่ใช่โครงสร้าง

---

## ข้อจำกัดที่รับทราบไว้ (ไม่ต้องแก้ตอนนี้)

- **`ts_key` ละเอียดแค่ 1 วินาที** — วัดสองชิ้นในวินาทีเดียวจะจับคู่ชนกัน ในทางปฏิบัติงานนี้กดมือทีละชิ้น แทบเป็นไปไม่ได้ ถ้าอนาคตต่อ conveyor อัตโนมัติต้องเปลี่ยนไปใช้เลข 10 หลักแทน
- **บรรทัดใน `.txt` อาจถูกจับคู่ซ้ำ** — ไม่มีการ mark ว่าใช้ไปแล้ว
- **#9 ทาง B** — measurement มีใน DB แต่ไม่มีรูป ไม่มีใครรู้ ต้องแก้แยก
- **`_find_measurement_for_image` อ่านไฟล์ใหม่ทั้งไฟล์ทุก 0.3 วิ** — เปลืองแต่ไฟล์เล็ก ยังไม่เป็นปัญหาจริง
- **`measured_count` ไม่ลดตอนลบ measurement** — ลบผ่านถังขยะแล้ว counter ค้างสูงถาวร มีผลตอนทำรายงานย้อนหลัง
