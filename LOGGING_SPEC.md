# แผน Logging — TM-X Pipeline

> ระบุทุกจุดที่จะเขียน log พร้อมระดับ · อ้างอิงเลขบรรทัดจากโค้ดปัจจุบัน (6 ส.ค. 2569)

---

## รูปแบบบรรทัด

```
2026-08-06 17:28:42.531 | INFO  | 260731_172842_0000000008 | ได้รูป HEAD-A (4.2 MB)
└──────── เวลา ────────┘ └level┘ └──── correlation key ────┘ └──── ข้อความ ────┘
```

### Correlation key — ต่างกันระหว่าง 2 ไฟล์

| ไฟล์ | key | ที่มา |
|---|---|---|
| `Recieve_tm-x.py` | `260731_172842_0000000008` | 3 field แรกของชื่อไฟล์รูป |
| `send_command(Pi).py` | `s7/p3` | `session_id` / `piece` |
| ไม่ผูกกับชิ้นงาน | `-` | banner, session watcher |

> ⚠️ **สองไฟล์ใช้ key คนละแบบ** เพราะ Pi ไม่เคยเห็นชื่อไฟล์ของ TM-X เลย
> เวลาไล่ปัญหาข้ามฝั่งต้องเทียบด้วย **เวลา** (นาฬิกา Pi กับ PC ต้องตรงกัน)
> ถ้าอนาคตอยากให้เชื่อมกันได้ตรงๆ ต้องให้ Backend ส่ง `client_uuid` กลับไปให้ Pi

---

## ไฟล์ที่จะเกิด

```
logs/
├── recieve_tmx-2026-08-06.log          ← INFO ขึ้นไป (ทุกอย่าง)
├── recieve_tmx-2026-08-06.error.log    ← WARNING ขึ้นไป
├── send_command-2026-08-06.log
└── send_command-2026-08-06.error.log
```

หมุนรายวัน เก็บ 30 วัน · `encoding='utf-8'` บังคับเสมอ (ไม่งั้นภาษาไทยเพี้ยนบน Windows)

---

# ส่วนที่ 1 — `Recieve_tm-x.py` (บน PC)

## 🟢 INFO

| บรรทัด | ฟังก์ชัน | ข้อความ | หมายเหตุ |
|---|---|---|---|
| 593-605 | `start_ftp_server` | banner ตอนเปิดโปรแกรม | ครั้งเดียว · บอกโหมด/พอร์ต/โฟลเดอร์ |
| **ใหม่** | `on_file_received` | `ได้ไฟล์ HEAD-A (4.2 MB)` | **ยังไม่มีในโหมดจริง** — จำเป็นเพราะเป็นจุดเริ่มจับเวลา |
| **ใหม่** | `on_file_received` | `ได้ไฟล์ .txt (312 bytes)` | ยืนยันว่า TM-X ส่งค่ามาแล้ว |
| 451 | `_handle_capture_inner` | `จับคู่ค่าได้ x=5.047 y=5.045 off=0.012` | |
| 474 | `_handle_capture_inner` | `บันทึกแล้ว id=42 result=OK (3/4)` | |
| 303 | `upload_image_to_backend` | `อัปโหลดรูปสำเร็จ (id=42)` | |
| **482** | `_handle_capture_inner` | `⏱ รอ .txt 0.40s · ถาม session 0.05s · POST 0.17s · อัปโหลด 0.35s · รวม 2.31s` | ⭐ **ตัวที่ใช้ตั้ง timeout** |
| 385 | `session_watcher` | `session เปลี่ยน 7 → 8` | |
| 389 | `session_watcher` | `session 7 จบแล้ว (state=stopped)` | |
| 357 | `clear_temp_dir` | `ล้าง Store_image_temporary แล้ว` | |
| 584, 587 | `start_ftp_server` | `ได้รับ Ctrl+C` / `ปิด FTP server เรียบร้อย` | |

## 🟡 WARNING — ข้ามชิ้นนี้ แต่ระบบยังปกติ

| บรรทัด | ข้อความ | กรณี | ความหมาย |
|---|---|---|---|
| 422 | `หาบรรทัดใน .txt ที่เวลาตรงไม่เจอ (รอครบ 5.00s) — ทิ้งรูป` | #3 #4 | TM-X ส่งรูปแต่ไม่ส่งค่า |
| 426 | `ชื่อไฟล์ไม่มีเวลาให้จับคู่ — ใช้บรรทัดล่าสุดแทน` | #5 | รูปแบบชื่อไฟล์เปลี่ยน |
| 429 | `ยังไม่มีไฟล์ .txt ให้อ่านเลย — ทิ้งรูป` | #5 | |
| 438 | `TM-X วัดไม่ติด (-9999.999) — ข้าม ไม่บันทึก` | #6 | |
| 445 | `ไม่มี session ที่ running — ทิ้งค่า/รูปนี้` | #7 | ของมาช้าหลัง session จบ |
| 340 | `ยังมีงานค้าง N รายการหลังรอ 30 วิ — ล้างต่อไป` | — | อาจมีรูปหาย |
| 351 | `ลบไฟล์ temp ไม่สำเร็จ` | — | ไฟล์ค้าง กินดิสก์ |
| **ใหม่** | `ได้รูปนอก head-a/head-b ติดกัน 5 ใบ — TM-X เปลี่ยน config?` | **#12** | ⭐ ตอนนี้เงียบสนิท |

## 🔴 ERROR — ระบบมีปัญหา ต้องมีคนดู

| บรรทัด | ข้อความ | กรณี | ผลกระทบ |
|---|---|---|---|
| 266 | `query /api/session/state ไม่สำเร็จ` | #8 | Backend ล่ม/เน็ตหลุด |
| 455 | `POST /api/measurements ไม่สำเร็จ — เก็บรูปไว้ไม่ลบ` | #9 | ค่าไม่เข้า DB |
| 469 | `Backend ปฏิเสธค่านี้ (HTTP 409/400/500)` | #10 | |
| 305 | `อัปโหลดรูปไม่สำเร็จ (HTTP xxx)` | **#9B** | ⚠️ **ค่าอยู่ใน DB แต่ไม่มีรูป — ตอนนี้หายเงียบ** |
| 307 | `อัปโหลดรูปไม่สำเร็จ (exception)` | **#9B** | เหมือนกัน |

## ⚪ DEBUG — ไม่เขียนออกมาโดยปกติ

| บรรทัด | ข้อความ |
|---|---|
| 336 | `รองาน N รายการที่ค้างอยู่ให้เสร็จก่อนล้าง` |
| 503-519 | รายละเอียดโหมดทดสอบ (`มีทั้งหมด N บรรทัด`, `บรรทัดล่าสุด: ...`) |

---

# ส่วนที่ 2 — `send_command(Pi).py` (บน Pi)

## 🟢 INFO

| บรรทัด | ข้อความ |
|---|---|
| 516-519 | banner ตอนเปิดโปรแกรม (พอร์ต, TM-X, Backend, timeout) |
| 315-321 | `ได้รับ Start: session=7 template='001' alpl=402 target=4` |
| 345 | `→ R0 : OK` |
| 350 | `→ PW,1,001 : OK` |
| 360 | `ชิ้นที่ 1/4 — รอสัญญาณ trigger` |
| 506 | `ได้รับสัญญาณ trigger` |
| 193 | `TM-X ตอบ trigger: OK` |
| 372 | `ชิ้นที่ 1/4 บันทึกแล้ว` |
| 260 | `รอผู้ใช้ตอบบนหน้าเว็บ (วัดต่อ/หยุด)` |
| 379 | `ผู้ใช้เลือกหยุด — จบ session` |
| 355, 365, 467 | `ได้รับคำสั่ง Stop` |
| 473 | `ส่ง S0 ไป TM-X แล้ว` |
| 403 | `จบ session — ปิดการเชื่อมต่อ TM-X` |
| 439 | `แจ้ง backend ปิด session แล้ว (วัดได้ 3/4 — ไม่ครบ)` |

## 🟡 WARNING

| บรรทัด | ข้อความ | หมายเหตุ |
|---|---|---|
| 377 | `รอ 15 วิแล้วไม่ได้รับค่าการวัด` | จุดที่ 10 กรณีมารวมกัน |
| 381 | `ผู้ใช้เลือกวัดต่อ — ไปชิ้นถัดไป` | **แปลว่ามีชิ้นถูกข้าม ข้อมูลไม่ครบ** |
| 443 | `แจ้งปิด session ไม่ได้` | heartbeat จะปิดให้แทน |
| 216 | `อ่าน session state ไม่ได้` | ⚠️ ดูหัวข้อ "ระวัง log ท่วม" ด้านล่าง |

## 🔴 ERROR

| บรรทัด | ข้อความ | กรณี |
|---|---|---|
| 326-327 | `Backend ไม่ได้ส่ง target_count มา` | ไม่เริ่มวัดเลย |
| 337-339 | `ต่อ TM-X ที่ 192.168.10.11:8600 ไม่ได้` | สาย LAN/เครื่องปิด |
| 196 | `ส่ง trigger ไม่สำเร็จ: timeout` | **#1** |
| 199 | `ส่ง trigger ไม่สำเร็จ: connection refused` | **#1** |
| 202 | `ส่ง trigger ไม่สำเร็จ: {exc}` | **#1** |
| 257 | `แจ้ง Backend เรื่อง timeout ไม่สำเร็จ — ถือว่าหยุด` | |
| 272 | `ถามคำตอบจาก Backend ไม่สำเร็จ — ถือว่าหยุด` | |
| 384 | `session พังกลางทาง — {type}: {exc}` | |
| **ใหม่** | `TM-X ตอบ ER,PW,xx — โหลดโปรแกรมไม่สำเร็จ` | ⭐ ดูด้านล่าง |

---

## ⭐ จุดใหม่ที่สำคัญที่สุด — เช็ค response

บรรทัด 345, 350, 193 ตอนนี้ **รับ response มาแล้ว `print` ทิ้ง** ไม่ได้เช็คอะไรเลย

TM-X ตอบ error ในรูปแบบ `ER,<คำสั่ง>,<รหัส>` (หลักฐาน: `ER,T1,03` ในคอมเมนต์ `agent.py:54`)

```
response.startswith("ER")  →  ERROR
```

| เช็คที่ | จับอะไร | ระดับ |
|---|---|---|
| `R0` (345) | TM-X ไม่พร้อม / โหมดผิด | ERROR |
| `PW` (350) | **โหลดโปรแกรมผิด/ไม่มีโปรแกรมนั้น** | ERROR — สาเหตุที่สงสัยของ `-9999` |
| `T1` (193) | **TM-X ไม่ได้รับ trigger** (#1) | ERROR |

---

## ⚠️ ระวัง log ท่วม — 1 จุดเดียว

`get_measured_count()` (บรรทัด 216) ถูกเรียกจาก `wait_for_measurement` ที่ **poll ทุก 0.4 วิ นาน 15 วิ = 37 ครั้งต่อชิ้น**

ถ้า Backend ล่ม → **37 บรรทัด WARNING ต่อชิ้นงาน 1 ชิ้น**

**ทางแก้**: log แค่ครั้งแรกของแต่ละรอบรอ แล้วสรุปตอนจบ

```
17:28:43 | WARN | s7/p3 | อ่าน session state ไม่ได้: ConnectError
17:28:58 | WARN | s7/p3 | อ่าน session state ไม่ได้ 37 ครั้งติดใน 15 วิ
```

> จุดอื่นไม่มีปัญหานี้ — `session_watcher` ใน `Recieve_tm-x.py` ใช้ `continue` เฉยๆ
> ไม่ได้พิมพ์ตอนล้มเหลว (บรรทัด 378) ซึ่งถูกต้องอยู่แล้ว

---

## ตัวอย่างไฟล์จริง

### กรณีปกติ — `recieve_tmx-2026-08-06.log`

```
17:28:42.531 | INFO | 260731_172842_0000000008 | ได้ไฟล์ HEAD-A (4.2 MB)
17:28:42.933 | INFO | 260731_172842_0000000008 | ได้ไฟล์ .txt (312 bytes)
17:28:42.935 | INFO | 260731_172842_0000000008 | จับคู่ค่าได้ x=5.047 y=5.045 off=0.012
17:28:43.102 | INFO | 260731_172842_0000000008 | บันทึกแล้ว id=42 result=OK (3/4)
17:28:43.455 | INFO | 260731_172842_0000000008 | อัปโหลดรูปสำเร็จ
17:28:43.456 | INFO | 260731_172842_0000000008 | ⏱ รอ .txt 0.40s · session 0.05s · POST 0.17s · รูป 0.35s · รวม 2.31s
```

**ไฟล์ `.error.log` ว่างเปล่า** = เมื่อวานไม่มีปัญหา

### กรณี TM-X วัดไม่ติด

```
# recieve_tmx.log
17:30:11.204 | INFO | 260731_173011_0000000009 | ได้ไฟล์ HEAD-A (4.2 MB)
17:30:11.610 | INFO | 260731_173011_0000000009 | ได้ไฟล์ .txt (334 bytes)
17:30:11.612 | WARN | 260731_173011_0000000009 | TM-X วัดไม่ติด (-9999.999, -9999.999) — ข้าม

# send_command.log — คนละเครื่อง เทียบด้วยเวลา
17:30:10.980 | INFO | s7/p4 | ได้รับสัญญาณ trigger
17:30:11.001 | INFO | s7/p4 | TM-X ตอบ trigger: OK
17:30:26.010 | WARN | s7/p4 | รอ 15 วิแล้วไม่ได้รับค่าการวัด
17:30:31.550 | WARN | s7/p4 | ผู้ใช้เลือกวัดต่อ — ไปชิ้นถัดไป
```

อ่านคู่กันแล้วเห็นชัด: **trigger ไปถึง · TM-X วัดจริง · แต่ค่าเป็น -9999** → ปัญหาอยู่ที่ฟิสิกส์/โปรแกรมวัด ไม่ใช่ระบบส่งข้อมูล

### กรณีอันตรายที่ตอนนี้หายเงียบ (#9B)

```
17:32:05.110 | INFO  | 260731_173205_0000000010 | บันทึกแล้ว id=43 result=OK (4/4)
17:32:35.230 | ERROR | 260731_173205_0000000010 | อัปโหลดรูปไม่สำเร็จ: ReadTimeout
```

`measured_count` เพิ่มครบ Pi เดินต่อปกติ หน้าเว็บขึ้น "no image yet"
**ตอนนี้ไม่มีใครรู้เลยว่ารูปหาย** — พอมี log อย่างน้อยตามหาย้อนหลังได้

---

## สรุปจำนวนจุด

| ไฟล์ | INFO | WARNING | ERROR | DEBUG | เพิ่มใหม่ |
|---|---|---|---|---|---|
| `Recieve_tm-x.py` | 11 | 8 | 5 | 2 | 3 |
| `send_command(Pi).py` | 13 | 4 | 8 | 0 | 1 |

**จุดที่เพิ่มใหม่ 4 จุด**
1. `ได้ไฟล์ HEAD-A / .txt` (INFO) — โหมดจริงยังไม่มี log ตอนรับไฟล์เลย
2. `รูปนอก head-a/head-b ติดกัน N ใบ` (WARNING) — กรณี #12 ตอนนี้เงียบสนิท
3. สรุปจำนวนครั้งที่ `อ่าน session state ไม่ได้` (WARNING) — กัน log ท่วม
4. `TM-X ตอบ ER,...` (ERROR) — เช็ค response ที่ `R0`/`PW`/`T1`

---

## ตั้งค่าใน `.env`

```
LOG_DIR=./logs
LOG_LEVEL=INFO              # ช่วงเก็บข้อมูล · เปลี่ยนเป็น WARNING ทีหลังได้
LOG_RETENTION_DAYS=30
LOG_FTP_DETAIL=1            # รับ log ของ pyftpdlib ด้วย (ได้ STOR seconds=)
```
