# แผนแก้: เกณฑ์ตัดสินแยกตามโหมด + ฟอร์มหลายกลุ่ม + Part เกิดพร้อม Measurement

> ตกลงกันแล้ว 6 ส.ค. 2569 · รวมงานที่ค้างจากหลายรอบเข้าเป็นแผนเดียว
> **อัปเดต 7 ส.ค. 2569** — ข้อ E ทดสอบผ่านแล้ว · เพิ่มข้อ F (Pi ตัดสิน OK/NG เอง)

---

## ภาพรวมสิ่งที่เปลี่ยน

```
เดิม                                  ใหม่
────────────────────────────────────  ────────────────────────────────────
เกณฑ์มาจาก part_number ทุกโหมด        IPM → package_size · New/Rework → part_number
offset ตัดสิน OK/NG ทุกโหมด           IPM ไม่ใช้ตัดสิน (โชว์เฉยๆ)
Part เกิดก่อนวัด                       Part เกิดพร้อม Measurement ใน transaction เดียว
1 ฟอร์ม = 1 package size              กด +Add ได้หลายกลุ่ม
IPM ต้องรู้ part_number                IPM ใช้แค่ package_size
Pi ไม่รู้ผลวัด รอ measured_count 15 วิ   Pi ดึงค่าเองด้วย GM แล้วบอก MCU ทันที
ค่าเดินทาง TM-X→FTP→PC→Backend        ค่ามาทาง TCP (Pi) · รูปมาทาง FTP (PC)
มี Pause ที่กดแล้วเครื่องไม่หยุดจริง      ตัด Pause ออกทั้งระบบ เหลือ Start/Stop
คำสั่งไปไม่ถึง Pi แล้วเงียบ              ขึ้น popup บอกสาเหตุจริง
Export กดได้ระหว่างวัด                  ล็อก Export ตอนมี session running
```

---

## 📎 เอกสารที่ต้องอ่านคู่กัน

แผนนี้บอกว่า **จะแก้อะไร** ส่วนรายละเอียดว่า **แก้ยังไง** แยกไป 3 ไฟล์

| ไฟล์ | ครอบคลุม | ขึ้นกับแผนนี้ไหม |
|---|---|---|
| **`FLOW_send_command.md`** | flow เป้าหมายของ `send_command.py` เล่าเป็นคำพูดทีละขั้น (7 ช่วง) พร้อมเหตุผลกำกับทุกจุด — **ข้อ E + F ของแผนนี้ต้องเขียนโค้ดตามไฟล์นั้น** | ✅ ใช่ |
| **`Handle_Pi_Error.md`** | เส้น **Backend → Pi** — คำสั่งไปไม่ถึง · ตัด Pause · heartbeat · ล็อก Export | ❌ **ทำแยกได้เลย** |
| **`Handle_Receive_Error.md`** | เส้น **Recieve ↔ Backend** — บันทึกว่าตอนนี้ handle ยังไงอยู่ (8 ข้อ) + ที่ยังขาด (2 ข้อ) | ❌ **ทำแยกได้เลย** |

> 2 ไฟล์ `Handle_*` ไม่ต้องรอ schema / `GM` / ฟอร์มหลายกลุ่ม — รวมกันแล้วเป็นงานไม่กี่สิบบรรทัด
> และฝั่ง Pi เป็นการ**ลบโค้ด**เสียส่วนใหญ่ ควรทำก่อนเพื่อนเลย

**ทั้ง 3 เส้นทางที่คุยกับกันข้ามโปรเซส มีเอกสารครบแล้ว**

```
Backend ──▶ Pi         Handle_Pi_Error.md
Pi      ──▶ TM-X       FLOW_send_command.md (ช่วง 2, 4, 5) + Test_T1_GM.py
Recieve ──▶ Backend    Handle_Receive_Error.md
```

---

## ✅ ตอบคำถาม: "Part บันทึกพร้อม Measurement อยู่ในแผนไหม"

**อยู่ครับ — เป็นข้อ C ด้านล่าง** และตอนนี้ทำได้สะอาดกว่าตอนคุยครั้งแรก เพราะ:

- `sessions.number_alpl` + FK ถูกถอดออกแล้ว → ไม่มีเหตุผลต้อง insert Part ตัวแรกล่วงหน้าอีก
- `package_size` มี `template_id` + เกณฑ์ของตัวเอง → **IPM ไม่ต้องพึ่ง `part_number` แล้ว**
  → ข้อกำหนดเดิมที่ว่า *"ต้องบังคับกรอก part_number ใน IPM"* **ตกไป** เหลือแค่ package_size

---

## A. Schema

### `package_size` — เพิ่ม 5 คอลัมน์

```sql
nominal_x   FLOAT NOT NULL,
nominal_y   FLOAT NOT NULL,
upper_tol   FLOAT NOT NULL,
lower_tol   FLOAT NOT NULL,
offset_tol  FLOAT NOT NULL,
```

> ❌ **ไม่เพิ่ม `part_number_id`** — ความสัมพันธ์คือ 1 package_size → หลาย part_number
> การใส่กลับจะกลายเป็น FK วนลูปและบังคับให้เป็น 1:1 ซึ่งขัดกับความจริง

### `parts_specifications` — เพิ่ม `package_size_id`

```sql
package_size_id INT,
FOREIGN KEY (package_size_id) REFERENCES package_size(package_size_id)
```

บทบาทต่างกันตามโหมด:

| โหมด | `part_number_id` | `package_size_id` |
|---|---|---|
| New / Rework | ✅ ตัวหลัก | สำเนาไว้ใช้สะดวก |
| IPM | `NULL` | ✅ **ตัวเดียวที่มี** |

**เก็บทั้งคู่เสมอ** แต่ต้องมาจากการเลือกครั้งเดียวกัน (ดูข้อ B4)

### ล้างข้อมูลเก่า

ตกลงว่าลบ DB เดิมทิ้งได้ (ยังเป็นช่วงทดสอบ) → แก้ `init.sql` อย่างเดียว ไม่ต้องเขียน `ALTER TABLE` migration

### ไฟล์ที่ต้องแก้

- `mysql-init/init.sql`
- `main.py` — CRUD ของ `/api/package-sizes` รับ/ส่ง 5 field ใหม่
- `Frontend/edit.html` — Lookup Tables › Package Size เพิ่ม 5 คอลัมน์ในตาราง + ฟอร์ม

---

## B. Backend — เกณฑ์แยกตามโหมด

### B1. `create_measurement` — เลือกแหล่งเกณฑ์

| โหมด | เกณฑ์จาก | offset ตัดสิน OK/NG |
|---|---|---|
| **IPM** | `package_size` | ❌ **คำนวณและเก็บ แต่ไม่เอามาตัดสิน** |
| **New / Rework** | `part_number` | ✅ ใช้ |

```
IPM:        result = OK ถ้า (ok_x และ ok_y)
New/Rework: result = OK ถ้า (ok_x และ ok_y และ ok_offset)
```

ค่า `offset` ยังถูก `INSERT` ลง DB เหมือนเดิมทุกโหมด — ต่างกันแค่ว่านับเป็นเกณฑ์หรือไม่

### B2. `_get_template_name_for_ipm` — ทางลัดใหม่

```
เดิม:  parts_specifications → part_number → package_size → template
ใหม่:  IPM ใช้ package_size → template ตรงๆ (ไม่ต้องผ่าน part_number เลย)
```

แก้ปัญหา *"ALPL ยังไม่ลงทะเบียน แล้วหา template ไม่เจอ"* ไปในตัว

### B3. `MEASUREMENTS_SELECT` — โชว์เกณฑ์ให้ถูกแหล่ง

เพิ่ม `LEFT JOIN package_size` แล้วเลือกใช้ตาม `measure_type` ของแถวนั้น
(ตอนนี้ join แต่ `part_number` → แถวที่เป็น IPM จะไม่มีเกณฑ์โชว์เลย)

### B4. เช็คความสอดคล้อง package_size ↔ part_number

ถ้า payload ส่งมาทั้งคู่ (New/Rework) ต้องตรวจว่า `part_number` ที่เลือกอยู่ใน `package_size` นั้นจริง
ไม่ตรง → ปฏิเสธ (กันข้อมูลขัดกันใน `parts_specifications`)

### B5. SSE event `measurement`

แนบธงบอกว่า offset นับเป็นเกณฑ์ไหม เพื่อให้หน้าเว็บแสดงหมายเหตุได้ถูก

---

## C. Part เกิดพร้อม Measurement

### C1. ลบจุดที่สร้าง Part ล่วงหน้า — **2 ที่**

| ไฟล์ | จุด | โหมด |
|---|---|---|
| `Frontend/index.html` | for-loop ยิง `POST /api/parts` (~2738-2754) | IPM |
| `Backend-server/main.py` | `_insert_part_row(cur, first_alpl, data)` (~771-774) | New |

### C2. `start_session` — เก็บ config ไว้ใช้ทีหลัง

```python
"new_part_config": data if measure_type in ("New", "IPM", "Rework") else None
```

พร้อม **validate โดยไม่ insert** (package_size / part_number มีจริงไหม) เพื่อให้รู้ตั้งแต่กด Start
ไม่ใช่ไปรู้ตอนวัดชิ้นแรกเสร็จ

### C3. `create_measurement` — ครอบ transaction เดียว

```python
db.autocommit(False)
try:
    INSERT parts_specifications   (ถ้ายังไม่มี)   ← บน
    INSERT measurements                          ← ล่าง (FK บังคับลำดับนี้)
    UPDATE measured_count + 1
    db.commit()
except:
    db.rollback()        # ไม่มี Part ผีตกค้าง
finally:
    db.autocommit(True)
```

> ตรรกะ `if new_part_config is not None` + `SELECT 1` เช็คก่อน insert **มีอยู่แล้ว**
> ALPL ที่ลงทะเบียนแล้วจะถูกข้ามอัตโนมัติ ไม่เขียนทับของเดิม

### ผลลัพธ์

```
กด Start → กด Stop ทันที        → ไม่มีอะไรเกิดขึ้นใน DB เลย
วัดชิ้นที่ 1 สำเร็จ                → Part + Measurement เกิดพร้อมกัน
วัดชิ้นที่ 1 ไม่ติด                → ไม่มีอะไรเกิดขึ้น
```

---

## D. ฟอร์มหลายกลุ่ม (+Add)

### D1. โครงสร้างกลุ่ม

| โหมด | field ต่อกลุ่ม | ALPL ต่อกลุ่ม |
|---|---|---|
| **IPM** | ALPL · Package Size | หลายตัว |
| **New** | ALPL · Package Size · Part Number · PO · Vendor · Owner · Description · Receive Date | หลายตัว |
| **Rework** | เหมือน New | หลายตัว |

`Operator` อยู่บนสุด **ตัวเดียวใช้ร่วมทุกกลุ่ม** (คนวัดคนเดียวกัน)

> **Rework หลาย ALPL ต่อกลุ่มได้แล้ว** — ยกเลิกข้อจำกัด `len(alpl_queue) != 1`
> เหตุผล: Rework คือชิ้นงานที่ส่งกลับไปให้ **vendor เดิม** แก้ไข → PO/Vendor/Owner
> เหมือนกันทั้งหมดอยู่แล้ว การเขียนทับ config ด้วยชุดเดียวกันจึงถูกต้อง
> ถ้าเป็นคนละชุดจริงๆ ผู้ใช้กด +Add แยกกลุ่มเอง

### D2. ⚠️ ทำเป็นกลไกกลางตัวเดียว อย่าเขียน 3 รอบ

ทั้ง 3 โหมดใช้โค้ด add/remove/validate/save-state ชุดเดียวกัน ต่างกันแค่**ลิสต์ field**
ถ้าทำแยกจะได้ตรรกะซ้ำ 3 ชุด แก้บั๊กทีต้องไล่ 3 ที่

### D3. งานฝั่ง Frontend

| จุด | แก้อะไร |
|---|---|
| HTML ฟอร์ม | id เดี่ยว → index-based (`ipm-0-alpl`) |
| `setFieldError` / `clearFieldErrors` | รองรับ error รายกลุ่ม |
| `savePartEntryState` / restore | เก็บ array ของกลุ่ม |
| `onSubmitIpm/New/Rework` | validate ต่อกลุ่ม + **เช็ค ALPL ซ้ำข้ามกลุ่ม** |
| `startFromQueue` | ส่งหลายกลุ่มไป backend |
| cascade dropdown | New/Rework ต้องทำต่อกลุ่ม (IPM ไม่ต้อง — ไม่มี Part Number แล้ว) |
| **ย่อกลุ่มที่กรอกเสร็จ** | เหลือบรรทัดเดียว ไม่ให้ modal สูงเกินจอ |

```
▸ กลุ่ม 1: ALPL 400,401,402,403 · 5x5 · TL1449HT      [แก้ไข] [ลบ]
▾ กลุ่ม 2: (กำลังกรอก — แสดงเต็ม)
```

### D4. Telemetry บน Dashboard

โหมด IPM ต้องแสดง Offset พร้อมหมายเหตุใต้กล่องว่า **"ไม่ได้นำมาเป็นเกณฑ์ตัดสิน"**

### D5. เกณฑ์ตรวจ ALPL ต่อโหมด + ฟอร์มหลังเตือน

> ตกลงกัน 7 ส.ค. 2569

**3 โหมดมีเงื่อนไขตรงข้ามกัน**

| โหมด | เงื่อนไข | เจอแล้วทำยังไง |
|---|---|---|
| **IPM** | ต้องมีใน DB | **ถามยืนยัน** → ลงทะเบียนให้ได้ (ฟอร์มมี Package Size พอสร้าง Part แล้ว) |
| **Rework** | ต้องมีใน DB | **บล็อก** — Rework คือส่งกลับ vendor เดิม ไม่มีในระบบ = ผิดแน่นอน |
| **New** | ต้อง**ไม่มี**ใน DB | **บล็อก** — ปล่อยผ่านจะเขียนทับ config เดิมของ Part ที่มีประวัติอยู่ |

#### 🔑 หลักการเดียวที่ใช้กับทั้ง 3 กรณี — **อย่าล้างสิ่งที่ผู้ใช้พิมพ์ไว้**

กรอก ALPL มา 20 ตัวแล้วผิดตัวเดียว ต้องไม่พิมพ์ใหม่หมด
ปิด popup แล้ว **เปิดฟอร์มกลับมาพร้อมข้อมูลครบเหมือนเดิม + ไฮไลต์ช่องที่ผิด**
(ตอนนี้ `index.html:2733` ทำถูกอยู่แล้ว — ต้องคงพฤติกรรมนี้ไว้ตอนย้ายไป React)

#### ข้อความที่ควรขึ้น

```
IPM     ⚠️ ALPL 402, 405 ยังไม่เคยบันทึกมาก่อน
           จะลงทะเบียนด้วย Package Size "5x5" แล้ววัดต่อเลยไหม
           [ลงทะเบียนแล้ววัดต่อ]  [กลับไปแก้]

Rework  ❌ ALPL 402 ไม่มีในระบบ — Rework ต้องเป็นชิ้นที่เคยวัดมาก่อนเท่านั้น
           ถ้าเป็นชิ้นใหม่ ให้เลือกโหมด "New" แทน
           [กลับไปแก้]

New     ❌ ALPL 402 มีอยู่ในระบบแล้ว (Part Number "XYZ-1", Vendor "ABC")
           ถ้าจะวัดซ้ำใช้ "IPM" · ถ้าเป็นงานแก้จาก vendor ใช้ "Rework"
           [กลับไปแก้]
```

**ต้องบอกเลข ALPL ที่มีปัญหาเสมอ** — สาเหตุที่เจอบ่อยที่สุดคือพิมพ์ผิดหลักเดียว
(`402` → `4020`) ถ้าไม่โชว์เลข ผู้ใช้จะกดยืนยันทั้งที่เป็น typo แล้วได้ Part ขยะใน DB

**บอกทางออกด้วย ไม่ใช่แค่บอกว่าผิด** — ทั้ง Rework และ New ที่ผิดเงื่อนไข
สาเหตุที่พบบ่อยคือ**เลือกโหมดผิด** ไม่ใช่กรอกเลขผิด

#### ❌ 2 ข้อห้าม

- **อย่าลบ ALPL ตัวที่ผิดออกให้อัตโนมัติ** — ผู้ใช้จะไม่รู้ว่าหายไปตัวไหน แล้วกด Start
  ทั้งที่ขาดชิ้น
- **อย่าเช็คตอนกด Start อย่างเดียว** — เช็คตอน blur ของช่อง ALPL ด้วย ผู้ใช้จะรู้ตัว
  ก่อนกรอกอีก 6 ช่องที่เหลือ

### D6. `POST /api/parts/check` — เลิกโหลด Part ทั้งตาราง

ตอนนี้หน้าเว็บโหลด Part **ทั้งหมด** มาไว้ในหน่วยความจำแล้วเทียบเอง
(`index.html:2560` วนดึงทีละ 1000 จนหมดตาราง) — Part ยิ่งเยอะยิ่งช้า และดึงมา
เพื่อเช็คแค่ไม่กี่ตัว

```
POST /api/parts/check
{"alpl": [400, 401, 402]}

→ {"exists":  [400, 401],
   "missing": [402],
   "detail":  {"400": {"part_number": "XYZ-1", "vendor": "ABC"}, ...}}
```

- **ใช้ได้ทั้ง 3 โหมด** — IPM/Rework ดู `missing` · New ดู `exists`
- `detail` มีไว้ให้ข้อความของโหมด New โชว์ config เดิมได้ (ดู D5)
- ตอบครั้งเดียวได้ทุกตัว ไม่ต้องยิงทีละ ALPL

> ⚠ ต้อง **ไม่** เรียก `_block_if_session_running()` — เป็นการอ่านอย่างเดียว
> และต้องใช้ได้ตอนที่ยังไม่ได้เริ่มวัด

---

## E. คิวพก template + Pi (✅ ทดสอบแล้ว 7 ส.ค. 2569)

### E1. `queue_state` เก็บ template ต่อชิ้น

```json
{"queue":     [400, 401, 402, 403, 500, 506, 509],
 "templates": ["001","001","001","001","007","007","007"],
 "position":  0}
```

**ใช้ 1 session เดียว** ไม่แยกเป็น 2 — dashboard ต่อเนื่อง · `measured_count` = 7/7 · `S0` ครั้งเดียวตอนจบ

### E2. Pi ส่ง `PW` เมื่อ template เปลี่ยน

```
R0 → PW(001) → T1×4 → PW(007) → T1×3 → S0
```

### ✅ ผลทดสอบ — `test_pw_switch.py`

**เปลี่ยนโปรแกรมกลางคันได้ ไม่ต้องคั่นด้วย `S0`/`R0`** → แผนข้อ E ใช้ได้ตามที่ออกแบบไว้

ทดสอบ `R0 → PW(021) → T1 → PW(020) → T1 → PW(021) → T1` ผ่านทุกขั้น

### 📚 กติกา TM-X ที่ค้นพบระหว่างทาง — กระทบโค้ดจริงทั้งหมด

| # | สิ่งที่พบ | โค้ดตอนนี้ | ต้องแก้เป็น |
|---|---|---|---|
| 1 | `R0` ตอบทันทีแต่โหมดยังไม่เปลี่ยน | `sleep(0.5)` เดาเอา | วน `RM` จนได้ `1` |
| 2 | `T1` ใช้ connection หลักได้ | เปิดสายที่สองทุกครั้ง | ใช้สายหลัก |
| 3 | `PW` ช้าคงที่ตามโปรแกรม | `settimeout(5.0)` | `20.0` เฉพาะ `PW` |
| 4 | `PW` พ่วง RESET → READY ปิดชั่วคราว | ยิง `T1` ทันที | retry เมื่อเจอ `ER,03` |
| 5 | `S0` ต่อคิวรองานที่ค้าง จึงช้า | `except: pass` กลืน error | log + timeout ยาวขึ้น |

**ข้อ 1 — `RM` (คู่มือหน้า 5-6)**

```
ส่ง : RM        รับ : RM,0 (โหมดตั้งค่า) · RM,1 (โหมดดำเนินการวัด)
```

คู่มือหน้า 5-4 ระบุว่า `R0` = *"เรียกใช้ข้อมูลคำสั่งทันที"* — ตอบกลับทันทีแต่การสลับ
โหมดจริงยังไม่เสร็จ นี่คือที่มาของ `ER,T1,03` ที่เจอมาตลอด (รหัส 03 = *"เมื่อมี
การออกคำสั่งในโหมดตั้งค่า"* ไม่ได้เกี่ยวกับ connection อย่างที่เคยเข้าใจ)

**ข้อ 2 — สาเหตุที่ `S0` ตอนจบ session ไม่เคยสำเร็จ**

TM-X ให้มีอุปกรณ์ควบคุมได้ทีละตัว การเปิดสายที่สองส่ง `T1` ทำให้ **สายเดิมถูกตัดทิ้ง**
→ `S0` ใน `finally` ยิงลงสายที่ตายแล้ว → `except: pass` กลืนไป → TM-X ค้างโหมดวัด
และจอหน้าเครื่องขึ้น *"Another device is currently in use"*
ต้องปิดด้วย `shutdown(SHUT_RDWR)` ก่อน `close()` ด้วย ไม่งั้น TM-X ไม่ปล่อยสิทธิ์คืนจอ

**ข้อ 3 — เวลาที่ `PW` ใช้จริง**

```
โปรแกรม 021 : 1.58 · 1.54 วิ    ← โหลดซ้ำหลังวัดไป 2 ชิ้น เวลาเท่าเดิม
โปรแกรม 020 : 3.72 วิ           ← เหลือ margin แค่ 1.28 วิจาก settimeout(5.0)
```

พิสูจน์แล้วว่า **ช้าเพราะตัวโปรแกรมเอง ไม่ใช่สะสมตามจำนวนชิ้นที่วัด** (ทดลองแทรก
`R0` ซ้ำ + หน่วง 5 วิก่อนยิง `PW` แล้วไม่ช่วยเลย · และ 021 รอบที่ 3 เร็วเท่ารอบแรก)
→ วัดหลายสิบชิ้นก็ไม่บานปลาย แต่ **โปรแกรมตัวอื่นที่ยังไม่เคยวัดอาจเกิน 5 วิ**
timeout เป็นเพดานไม่ใช่การหน่วง ตั้ง 20 วิไปเลยไม่มีข้อเสีย

---

## F. Pi ตัดสิน OK/NG เอง ผ่าน `GM` เพื่อสั่ง MCU

> ตกลงกัน 7 ส.ค. 2569 · ⏸ รอผล `Test_T1_GM.py` ก่อนเริ่ม

### F0. ทำไม

MCU ต้องรู้ OK/NG **ทันที** เพื่อคัดแยกชิ้นงาน ถ้าให้ Pi ไปถาม backend ค่าต้องเดินทาง
`TM-X → FTP → PC → POST → Backend → Pi ถาม` ซึ่งไม่มีเวลารับประกัน และถ้า FTP พลาด
รอบนั้น MCU จะไม่ได้คำตอบเลย

`GM` ดึงค่าได้บนสาย TCP เส้นเดียวกับที่ยิง `T1` — ไม่พึ่ง FTP ไม่พึ่ง backend

```
ส่ง : GM,3,0                              (o=3 ค่า+สถานะ+ผลตัดสิน · t=0 ทุกเครื่องมือ)
รับ : GM,t,m,i,j,…,m,i,j
        m = ค่าที่วัดได้        i = สถานะ (1 = ค่าปกติ)
        j = ผลตัดสินของ TM-X เอง (0 OK · 1 NG)
```

### F1. `main.py` — เพิ่ม `_build_groups()` แล้วแนบไปกับ `/command`

`_notify_agent_start()` (`main.py:632`) ส่ง payload เดิม + ฟิลด์ใหม่ `groups`
**ไม่ต้องสร้าง endpoint ใหม่**

```json
{
  "action": "start", "session_id": 42, "target_count": 6,
  "groups": [
    {"template_name": "021", "alpl": [400, 401, 402],
     "limits": {"x_lo": 5.009999, "x_hi": 5.030001,
                "y_lo": 3.389999, "y_hi": 3.410001, "offset_max": null}},
    {"template_name": "007", "alpl": [501, 502, 503],
     "limits": {"x_lo": 3.199999, "x_hi": 3.240001,
                "y_lo": 3.199999, "y_hi": 3.240001, "offset_max": 0.03}}
  ]
}
```

**ส่งขอบเขตสำเร็จรูป ไม่ส่ง `nominal`/`tol` ดิบ** — จำนวนตัวเลขเท่ากัน (4 ตัว) แต่
Pi ไม่ต้องลอก `_TOL_EPS = 1e-6` (`main.py:87`) ที่มีไว้กันคอลัมน์ `FLOAT`
(`5.02` อ่านกลับได้ `5.0199999809265137`) ลืมเมื่อไหร่จะตัดสินไม่ตรงกับ backend
**เฉพาะชิ้นที่ตกขอบพอดี** ซึ่งเป็นชิ้นที่สำคัญที่สุดและหาสาเหตุยากที่สุด

```python
x_lo = nominal_x - lower_tol - _TOL_EPS      # ใช้ค่าเต็ม ห้ามปัดเศษให้สวย
x_hi = nominal_x + upper_tol + _TOL_EPS      # ต้องเป็นตัวเลขชุดเดียวกับที่ backend ใช้
```

**ไม่ส่ง `measure_type`** — `offset_max: null` คือคำตอบอยู่แล้วว่าไม่ต้องตรวจข้อนี้
ส่งไปด้วยเปิดช่องให้มีคนเขียน `if measure_type == "IPM"` ที่ฝั่ง Pi แล้วกฎจะอยู่ 2 ที่
(New กับ Rework ลง DB เป็น `measure_type='New'` เหมือนกันอยู่แล้ว Pi แยกไม่ออกตั้งแต่แรก)

**ไม่ส่ง `queue` แยก** — เป็น `[a for g in groups for a in g["alpl"]]` บรรทัดเดียว
ส่งซ้ำมีแต่จะเสี่ยงไม่ตรงกันเอง

**payload ของ IPM กับ New/Rework หน้าตาเหมือนกันเป๊ะ** ต่างแค่ตัวเลข → Pi มี code path
เดียว ตรงกับที่เป็นอยู่แล้ววันนี้ (Pi ไม่เคยรู้เรื่องโหมด)

#### สิ่งที่ `_build_groups()` ต้อง validate ก่อนส่ง

| เช็ค | ทำไม |
|---|---|
| ทุก ALPL ในกลุ่มมี `nominal`/`tol` เท่ากันจริง | ดู ⚠ ด้านล่าง |
| `sum(len(g.alpl)) == target_count` | กันคิวกับเกณฑ์เหลื่อมกัน |
| ไม่มีกลุ่มที่ `alpl` ว่าง | `PW` โดนยิงฟรีโดยไม่มีชิ้นตาม |
| ไม่มี ALPL ซ้ำข้ามกลุ่ม | — |

> ⚠️ **สคีมาไม่ได้บังคับว่า package_size เดียวกันต้อง tol เท่ากัน**
> `tolerance` อยู่ที่ `part_number` ส่วน `template` อยู่ที่ `package_size` — ในทางใช้งานจริง
> package_size คือขนาดบอดี้ซึ่งกำหนด nominal อยู่แล้ว กลุ่มเดียวกันจึงเหมือนกันหมด
> **แต่ถ้าวันหนึ่งมีคนกรอกผิดที่หน้า Edit มันจะเงียบ** — Pi คัดของตามเกณฑ์กลุ่ม
> backend บันทึกตามเกณฑ์รายตัว (`create_measurement` query ใหม่ทุกชิ้น `main.py:2251`)
> ไม่มีใครรู้จนกว่าจะไปนับของจริง เช็ค 3 บรรทัดเปลี่ยนอาการนั้นเป็นข้อความตอนกด Start:
>
> ```
> เริ่มวัดไม่ได้ — ALPL 402 ใช้ Part Number "XYZ-2" ซึ่งมี tolerance ต่างจากตัวอื่น
> ในช่องเดียวกัน (±0.02 vs ±0.01) — แยกไปกรอกคนละช่อง
> ```

### F2. `main.py` — `_offset_limit()` ตัวกลางตัวเดียว

ตอนนี้ `create_measurement` ตรวจ offset **ทุกโหมด** ไม่ได้แยก IPM เลย (`main.py:2265`)

```python
ok_offset = _offset_ok(req.offset, part.get("offset_tol"))
```

`_offset_ok` ผ่านเสมอก็ต่อเมื่อ `offset_tol` เป็น `NULL` ในตาราง — **ไม่ได้ผูกกับ
measure_type** ถ้าปล่อยไว้แล้วส่ง `offset_max: null` ให้ Pi ตามข้อ B1 จะได้:

```
Pi      → offset_max null → ไม่ตรวจ → บอก MCU ว่า OK → ของไหลไปฝั่งดี
Backend → offset_tol 0.05 → ตรวจ    → บันทึก NG
```

ของจริงกับรายงานสวนทางกันโดยไม่มี error — ต้องทำฟังก์ชันกลางที่ **ทั้ง `_build_groups()`
และ `create_measurement` เรียกตัวเดียวกัน** (หลักเดียวกับ `_render_report` ที่เป็นตัวกลาง
ของ preview/Excel/PDF เพราะเคยเจอแล้วว่าถ้าแยกกันคำนวณ สิ่งที่เห็นกับสิ่งที่ได้จะเพี้ยนเงียบๆ)

```python
def _offset_limit(measure_type: str, part_row) -> float | None:
    """เพดาน offset ที่ต้องตรวจ — None = ไม่ตรวจข้อนี้"""
    if measure_type == "IPM":
        return None                       # ← กฎข้อ B1 อยู่ที่นี่ที่เดียว
    return part_row.get("offset_tol")
```

### F3. `send_command(Pi).py` — ลูปใหม่

> 📎 **flow เต็มอยู่ใน `FLOW_send_command.md`** — เขียนเป็นคำพูดทีละขั้น 7 ช่วง
> พร้อมเหตุผลว่าทำไมแต่ละขั้นต้องเป็นแบบนั้น **ให้เขียนโค้ดตามไฟล์นั้น** ตารางข้างล่าง
> เป็นแค่สรุปว่าไปแตะโค้ดเดิมตรงไหนบ้าง

```python
for g in groups:                                    # กลุ่ม = 1 รอบ PW
    send_cmd(sock, f"PW,1,{g.template_name.zfill(3)}", timeout=20.0)
    wait_for_mode(sock, 1)                          # RM ยืนยัน แทน sleep
    for alpl in g.alpl:                             # ชิ้นในกลุ่ม
        wait_for_trigger()                          # ของเดิม ไม่ต้องแก้
        clear_measurement(sock)                     # MRS ล้างค่าเก่า
        send_cmd(sock, "T1")                        # retry ถ้าเจอ ER,03
        x, y, offset = poll_gm(sock)                # วนจนได้ค่าใหม่
        ok = judge(x, y, offset, g.limits)
        notify_mcu(ok)                              # ← ของใหม่ ยังไม่มีในระบบ
        post_measurement(session_id, x, y, offset)
```

| จุดที่แก้ | เดิม | ใหม่ |
|---|---|---|
| `CommandRequest` (`:474`) | 5 ฟิลด์ | + `groups: list[Group] \| None` |
| `command_flow()` (`:295`) | วน `range(target_count)` แบน | วน 2 ชั้น group → ชิ้น |
| `trigger_sensor()` (`:157`) | เปิด socket ใหม่ส่ง `T1` | ใช้ `sock` หลัก (กติกา E ข้อ 2) |
| `send_command()` (`:145`) | `sleep(0.1)` + `recv` เดียว | วน `recv` จนเจอ `\r` (ดู `Test_T1_GM.py`) |
| `wait_for_measurement()` (`:223`) | poll `measured_count` 15 วิ | **ลบทิ้ง** — `GM` บอกทันทีแล้ว |
| หลัง `R0`/`PW` | `sleep()` | `wait_for_mode(sock, 1)` |
| `finally` | `S0` แล้ว `except: pass` | log ผล + `shutdown(SHUT_RDWR)` |

**`wait_for_trigger()` (`:123`) ไม่ต้องแก้เลย** — ยังเป็น `Event` ที่รับสัญญาณจาก
`/trigger` หรือ MCU เหมือนเดิม

**`ask_user_on_timeout()` ยังต้องมี** แต่เงื่อนไขเปลี่ยนจาก *"`measured_count` ไม่ขยับ"*
เป็น *"`GM` ไม่คืนค่าใหม่ภายใน `GM_MAX_WAIT`"* — เร็วขึ้นจาก 15 วิเหลือระดับวินาที

#### ⚠ `MRS` ก่อน `T1` ทุกครั้ง — ห้ามลืม

`GM` ดึง *"ค่าของภาพล่าสุด"* และ **ไม่มีเลขลำดับกำกับ** มองไม่ออกว่าเป็นของชิ้นใหม่
หรือชิ้นก่อน ยิ่ง `T1` ตอบกลับตอน *รับทริกเกอร์* ไม่ใช่ตอนวัดเสร็จ (คู่มือหน้า 5-4)
ยิง `GM` ตามติดจึงได้ `ER,GM,03` (ดี รู้ตัว) **หรือค่าของชิ้นก่อนหน้า (อันตราย เงียบสนิท)**

`MRS` ล้างค่าเก่าก่อน ทำให้ระหว่างรอ `GM` ต้องตอบ "ไม่มีค่า" เท่านั้น ได้ค่าเมื่อไหร่
= ของชิ้นใหม่แน่นอน

> บั๊กนี้มีอยู่แล้วใน `tcp.py` (legacy) — ยิง `T1` แล้ว `GM` ติดกัน กรอง `9999.999` ทิ้ง
> แล้ว `print(values[-2:])` ซึ่งพิมพ์ค่าของ**รอบก่อน**ออกมาเหมือนสำเร็จ

### F4. `Recieve_tm-x.py` — เหลือหน้าที่แค่รูป

ถ้า Pi เป็นคน POST ค่า จะมี **2 ทางที่ค่าเดียวกันวิ่งเข้า backend** (Pi ทาง TCP ·
PC ทาง FTP) → ได้ measurement ซ้ำ 2 แถวต่อชิ้น **ต้องเลือกทางเดียว**

**เลือกให้ Pi เป็นคน POST ค่า** เพราะการจับคู่ *"บรรทัดล่าสุดของ `.txt` กับรูปที่เพิ่งได้"*
คือจุดที่เปราะที่สุดในระบบตอนนี้ — ตัดทิ้งได้เลย

```
เดิม  Recieve: อ่าน .txt + จับคู่รูป → POST ค่า → upload รูป
ใหม่  Recieve: รับรูปอย่างเดียว → ถาม backend ว่า measurement ล่าสุดของ session คือ id ไหน
              → upload เข้า /api/measurements/{id}/image-upload (endpoint มีอยู่แล้ว)
```

ต้องยอมรับว่ารูปกับค่าจะไม่ atomic — รูปไม่มา = ได้ measurement ที่ `image_path` เป็น
`NULL` ซึ่ง **ดีกว่าตอนนี้** ที่ได้ "ไม่มีอะไรเลย" แล้ว `measured_count` ไม่ขยับ session ค้าง

### F5. 🔬 ต้องรู้ก่อนเริ่ม — `Test_T1_GM.py`

| คำถาม | กระทบอะไร |
|---|---|
| ต้องรอกี่ ms หลัง `T1` | ค่า `GM_MAX_WAIT` |
| `MRS`/`MSR` ล้างค่าเก่าได้จริงไหม | ถ้าไม่ได้ ต้องหาวิธีอื่นแยกค่าใหม่/เก่า |
| `GM,3,0` คืนค่าเรียงยังไง | ตัวไหนคือ `x`/`y`/`offset` |

**ข้อ 3 สำคัญที่สุด** — หยิบสลับกันแล้วตัดสินผิดทุกชิ้นโดยไม่มี error อะไรเลย
(คู่มือพิมพ์ชื่อคำสั่งล้างไม่ตรงกันเอง หน้า 5-9 หัวข้อเขียน `MRS` แต่ช่องส่ง/รับเขียน `MSR`
สคริปต์จึงลองทั้งคู่)

### ~~F6. ของแถม — `j` เป็นตัวจับ config drift~~ ❌ **ตกไปแล้ว**

> ทดสอบจริง 7 ส.ค. 2569 — `j` เป็น `0` (OK) **ทุกตัวทุกชิ้น** รวมถึงเครื่องมือที่
> `i=3` ไม่มีข้อมูลเลยก็ยังบอก OK
>
> แปลว่า **โปรแกรม 021 ไม่ได้ตั้ง tolerance ไว้ในตัว TM-X** ทุกอย่างจึงผ่านโดยปริยาย
> เอามาเทียบกับเกณฑ์ใน DB ไม่ได้
>
> จะกลับมาใช้ได้ก็ต่อเมื่อไปตั้ง tolerance ในโปรแกรมวัดที่หน้าเครื่องก่อน
> ซึ่งเป็นงานคนละเรื่อง — ดูผลเทสต์เต็มใน `IMPROVEMENT_PLAN.md`

### ⚠ F7. อุปสรรคที่เจอจากผลเทสต์ — `offset` อาจไม่ได้อยู่ใน `GM`

`GM,3,0` คืนมา **8 เครื่องมือ** (มีค่าจริง 6 · ไม่มีค่าถาวร 2) แต่ `.txt` ที่ระบบใช้อยู่
มีแค่ 3 ช่อง (`x`, `y`, `offset`)

```
[0] +5.065  [1] +5.076   ← น่าจะเป็นคู่ x/y (ใกล้เคียง .txt ที่ได้ 5.047/5.045)
[2] +1.979  [3] +2.141
[4] +16.969 [5] +20.701
[6] ·  [7] ·  ไม่มีค่า
```

**ไม่มีตัวไหนใกล้ `offset` = 0.003 เลย** — เป็นไปได้ว่า TM-X คำนวณตอนเขียนไฟล์

**ถ้าจริง ข้อ F ทั้งหมดต้องคิดใหม่** เพราะ Pi จะตัดสินได้แค่ 2 ใน 3 ข้อ
(`New`/`Rework` ที่ต้องเช็ค `offset_max` จะตัดสินไม่ตรงกับ Backend ทุกครั้งที่ชิ้นงานเยื้องเกิน)

**ต้องตอบคำถามข้อ 8b ก่อนเริ่ม F** — วิธีเร็วสุดคือเปิดโปรแกรม 021 ดูรายการเครื่องมือที่จอ TM-X

---

## ลำดับการทำ

| # | งาน | สถานะ |
|---|---|---|
| **0** | **`Handle_Pi_Error.md` ข้อ 1-3** — ตัด Pause · Pi ตอบ 400 · ล็อก Export | ✅ **เสร็จ 7 ส.ค. 2569** |
| 1 | Schema + ล้าง DB | ✅ **เสร็จ 12 ส.ค. 2569** |
| 2 | Backend: เกณฑ์แยกตามโหมด + `_offset_limit()` ตัวกลาง (B1 + F2) | ✅ **เสร็จ 12 ส.ค. 2569** |
| 3 | Frontend: โชว์ Offset + หมายเหตุ | ✅ **เสร็จ 12 ส.ค. 2569** |
| 4 | Part เกิดพร้อม Measurement (ข้อ C) | ✅ **เสร็จ 12 ส.ค. 2569** |
| — | **ถึงตรงนี้ได้คุณค่าหลักครบ** | |
| 5 | กลไกกลุ่มกลาง (นำร่องด้วย IPM) | ✅ **เสร็จ 12 ส.ค. 2569** — ทำรวดเดียวทั้ง 3 โหมด |
| 6 | ขยายไป New/Rework | ✅ รวมอยู่ในข้อ 5 |
| 6.5 | `POST /api/parts/check` + เกณฑ์ตรวจ ALPL ต่อโหมด (D5 + D6) | ✅ **เสร็จ 12 ส.ค. 2569** |
| 7 | Pi: กติกา TM-X (`RM` · `T1` สายหลัก · `PW` timeout · log `S0`) | ⬜ ทำแยกได้ทันที |
| 8 | คิวพก template + `_build_groups()` + payload (E + F1) | ⬜ โครงพร้อมแล้ว (ดูหมายเหตุ) |
| 9 | Pi ตัดสิน OK/NG ผ่าน `GM` + สั่ง MCU (F3) | ⬜ ติด F7 |
| 10 | `Recieve_tm-x.py` เหลือแค่รูป (F4) | ⬜ ต้องทำคู่กับข้อ 9 |
| 11 | `Handle_Pi_Error.md` ข้อ 4-5 + ส่วนที่ 2 | ⬜ เมื่อเจอปัญหาจริง |
| 12 | `Handle_Receive_Error.md` ส่วนที่ 2 (~11 บรรทัด) | ⬜ ทำแทรกได้ทุกเมื่อ |

> ⚠ **ข้อจำกัดที่ยังเหลือหลังทำข้อ 5-6.5 เสร็จ** — ฟอร์มกรอกหลายกลุ่มได้แล้ว
> แต่ **ทุกกลุ่มต้องได้ Template ตัวเดียวกัน** ไม่งั้น `start_session` ปฏิเสธ
> พร้อมบอกเหตุผล เพราะ Pi ยังรับ `template_name` ตัวเดียวตอน start แล้วส่ง `PW`
> ครั้งเดียว (การสลับ `PW` กลางคิว = ข้อ 8) ถ้าปล่อยผ่าน กลุ่มที่ 2 เป็นต้นไปจะ
> ถูกวัดด้วยโปรแกรมของกลุ่มแรก — ได้ค่าที่ "ดูเหมือนใช้ได้" แต่ผิดทั้งกลุ่ม
> โดยไม่มีอะไรเตือน
>
> `queue_state` เก็บ `groups` / `group_of` / `group_templates` แยกรายกลุ่มไว้แล้ว
> ตั้งแต่ตอนนี้ ข้อ 8 จึงเหลือแค่ "ส่งไปให้ Pi + ให้ Pi ยิง PW เมื่อ template
> เปลี่ยน" แล้วปลดเช็คตัวนี้ออก ไม่ต้องรื้อโครงสร้างใหม่

- **ข้อ 0 ทำก่อนเลย** — ส่วนใหญ่เป็นการลบโค้ด ใช้เวลาไม่กี่นาที และปิดเคส
  "ระบบโกหก" (กด Pause แล้วเครื่องวัดต่อ) ที่อันตรายที่สุดในระบบตอนนี้
- ข้อ 1-4 ทำต่อกันได้เลย ไม่ต้องรออะไร
- **ข้อ 7 ทำแยกได้ทันทีเช่นกัน** — เป็นการแก้บั๊กที่มีอยู่จริงวันนี้
  (`S0` ไม่เคยสำเร็จ · `ER,T1,03` · `PW` เฉียด timeout) ไม่ต้องรอ schema หรือ frontend
- ข้อ 8-9 ต้องเขียนตาม `FLOW_send_command.md`
- ข้อ 9-10 ต้องทำคู่กัน ถ้าทำข้อ 9 อย่างเดียวจะได้ measurement ซ้ำ 2 แถวต่อชิ้น (ดู F4)

> ⚠️ ข้อ 0 ตัด Pause ออก แปลว่า **`state` เหลือ 4 ค่า** (`idle`/`running`/`stopped`/`timeout`)
> ถ้าทำข้อ 1 (schema) ก่อน อย่าลืมเอา `paused` ออกจาก `init.sql` ไปพร้อมกัน

---

## ไม่อยู่ในแผนนี้ (ค้างไว้ก่อน)

- `send_command(Pi).py` — `number_alpl` 4 จุด (จะหายไปเองตอนทำข้อ 8 เพราะ `groups`
  พก `alpl` มาให้แล้ว)
- `client_uuid` = `ts_key` + เลข 10 หลัก (ดู `IMPROVEMENT_PLAN.md`)
- Logging เข้าไฟล์ (ดู `LOGGING_SPEC.md`)
- โปรโตคอลระหว่าง Pi ↔ MCU (`notify_mcu()` ในข้อ F3 ยังเป็นแค่ช่องว่างไว้)
