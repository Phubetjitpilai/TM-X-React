# Handle_Receive_Error — `Recieve_tm-x.py` รับมือความผิดพลาดยังไง

> เขียน 7 ส.ค. 2569 · ครึ่งแรกคือ **สิ่งที่โค้ดทำอยู่แล้ววันนี้** (พร้อมเลขบรรทัดให้ตามไปอ่าน)
> ครึ่งหลังคือสิ่งที่ยังขาด · ไฟล์อยู่ที่ `Backend-server/Recieve_tm-x.py`
>
> คู่กับ `Handle_Pi_Error.md` ซึ่งดูแลอีกเส้นทางหนึ่ง (Backend → Pi)

---

## Recieve คุยกับใครบ้าง

```
TM-X ──FTP(.txt + รูป)──▶ Recieve_tm-x.py ──HTTP──▶ Backend ──▶ MySQL
                                (คนละ process บนเครื่อง PC เดียวกัน)
```

**คนละไฟล์ = คนละ process = ไม่แชร์ตัวแปรกันเลย** ต่อให้อยู่เครื่องเดียวกัน
จึงต้องคุยผ่าน HTTP เหมือนกับที่ Pi คุยกับ Backend ทุกประการ ต่างกันแค่ปลายทาง
เป็น `localhost` (`BACKEND_URL` — `:69`) ไบต์วิ่งผ่าน loopback ไม่ออกสาย LAN จริง

### Recieve → Backend (3 อย่าง)

| ยิงอะไร | ตอนไหน | บรรทัด |
|---|---|---|
| `GET /api/session/state` | ทุกครั้งที่ได้รูป + ทุก 3 วิจาก `session_watcher` | `:267` `:383` |
| `POST /api/measurements` | ผ่านครบ 4 ด่าน | `:283` |
| `POST /api/measurements/{id}/image-upload` | ต่อจากข้อบน (multipart) | `:304` |

### Backend → Recieve

**ไม่มีเลย** — `Recieve_tm-x.py` ไม่มี HTTP server ในตัว ต่างจาก Pi ที่เปิดพอร์ต
`9998` รอรับ `/command` Backend จึงไม่มีทางสั่งอะไรมันได้

**นี่คือเหตุผลที่ต้องมี `session_watcher()` (`:368`) นั่ง poll ทุก 3 วิ** — ตอน session
จบ ไม่มีไฟล์อะไรส่งมาให้ Recieve เลย ถ้าไม่เฝ้าดูเอง จะไม่มีวันรู้ว่าจบแล้ว

> ทำไมอัปโหลดรูปผ่าน HTTP ทั้งที่ไฟล์อยู่บนดิสก์เดียวกัน — ให้ Backend อ่าน path ตรงๆ
> จะเร็วกว่าระดับมิลลิวินาที แต่ต้องแลกกับ 3 อย่าง: เปิดช่อง path traversal (Backend
> ต้องเชื่อ path จากโปรเซสอื่น) · เจ้าของไฟล์ temp กำกวมว่าใครลบ · ย้าย Recieve
> ไปคนละเครื่องไม่ได้อีก **สรุปว่าคงไว้แบบนี้**

---

# ส่วนที่ 1 — ที่ทำอยู่แล้ววันนี้

## 1.1 ด่านกรอง 4 ชั้นก่อนบันทึก (`_handle_capture_inner` — `:416`)

ทุกครั้งที่ได้รูปมา 1 ใบ ต้องผ่านครบ 4 ด่าน ตกด่านไหนก็ทิ้งแล้วจบ

| ด่าน | เช็คอะไร | ตกแล้วทำยังไง | บรรทัด |
|---|---|---|---|
| 1 | จับคู่ค่ากับรูปได้ไหม | ทิ้งรูป | `:424-438` |
| 2 | ค่าไม่ใช่ `-9999.999` (วัดไม่ติด) | ทิ้งรูป ไม่บันทึก | `:443-447` |
| 3 | มี session `running` อยู่ไหม | ทิ้งค่า+รูป | `:449-454` |
| 4 | Backend รับค่าไหม | **เก็บรูปไว้ไม่ลบ** | `:457-478` |

ด่าน 2 สำคัญกว่าที่คิด — ข้อมูลหน้างาน 31/07 พบว่า TM-X **วัดไม่ติด 7 จาก 8 ครั้ง**
ถ้าไม่กรองออก DB จะเต็มไปด้วย `-9999.999`

## 1.2 จับคู่รูปกับค่าด้วยเวลาในชื่อไฟล์ (`:220`)

TM-X ส่งของมา 3 ชิ้นต่อการวัด 1 ครั้ง **โดยรูปที่เราใช้มาถึงก่อนไฟล์ค่า**

```
HEAD-A/260731_172842_..._HEAD-A_OK.bmp     4.2 MB  ★ ใช้ใบนี้   ← มาก่อน
260731_..._result.txt                               ไฟล์ค่า      ← มาทีหลัง
260731_172842_..._capture-image_OK.bmp     2.3 MB    ข้าม
```

`_find_measurement_for_image()` จึง **วนรอไฟล์ `.txt` ได้ถึง 5 วินาที** (`TXT_WAIT_TIMEOUT`)
แทนที่จะอ่านทันทีแล้วทิ้ง — มีคำเตือนในโค้ดว่าห้ามเปลี่ยนเป็นอ่านทันทีเด็ดขาด (`:106-108`)

จับคู่ด้วย `ts_key` (`260731_172842`) ที่แกะจากชื่อไฟล์รูป แล้วไปหาบรรทัดที่มีเวลาตรงกัน
**ไม่ใช่ "เอาบรรทัดล่าสุด"** ซึ่งเป็นวิธีเดิมที่แปะผิดได้ถ้าค่ามาไม่เรียง

`_fallback_last_line()` (`:240`) เหลือไว้เป็นแผนสำรองกรณีชื่อไฟล์ไม่มีเวลาให้แกะ

## 1.3 เลือกรูปใบเดียวจาก 2 ใบ (`:100-109`)

TM-X ส่งรูป 2 ใบต่อการวัด 1 ครั้ง — กรองด้วยชื่อโฟลเดอร์ `HEAD-A` เท่านั้น
ถ้าไม่กรอง **วัด 1 ครั้งจะกลายเป็นบันทึก 2 ชิ้น**

## 1.4 ตัวนับงานค้าง กันลบไฟล์กลางคัน (`:131-158`)

```
POST /api/measurements → measured 4/4 → backend ปิด session ทันที
upload รูป 4 MB ...      ← session_watcher ตื่นมาเห็น stopped แล้วลบไฟล์กลางคัน
```

ผลคือ **ชิ้นสุดท้ายของทุก session เสี่ยงไม่มีรูป แบบสุ่มๆ หาสาเหตุยากมาก**

`_jobs_in_flight` นับตั้งแต่ได้รูปจนอัปโหลดเสร็จ/ทิ้งไปแล้ว — `clear_temp_dir()`
ต้องรอจนตัวนับเป็น 0 ก่อนลบเสมอ (รอสูงสุด 30 วิ) และ `_job_end()` อยู่ใน `finally`
ของ `_handle_capture()` (`:412`) จึงลดตัวนับเสมอไม่ว่าจะจบทางไหน

## 1.5 เช็ค `status_code` ก่อนแตะ `measurement_id` (`:466-478`)

```python
if resp.status_code != 200:
    ...
    return
data = resp.json()
upload_image_to_backend(data["measurement_id"], image_path)
```

ถ้า Backend ตอบ error (400 / 404 / 409) body จะเป็น `{"detail": ...}` ไม่มีคีย์
`measurement_id` → `KeyError` **ในเธรด daemon = ตายเงียบ** แล้วรูปค้างใน temp

## 1.6 ล้างโฟลเดอร์ temp อย่างปลอดภัย (`:326`)

- ลบทั้ง tree เพราะ TM-X สร้างโฟลเดอร์ย่อยเองเรื่อยๆ
- **เก็บตัวโฟลเดอร์แม่ไว้** เพราะ FTP server ผูก home directory ไว้กับ path นี้
  ถ้าลบทิ้งทั้งอัน TM-X ล็อกอินเข้ามาใหม่จะเจอ error
- ยึด `TEMP_IMAGE_DIR` เป็น absolute path ที่ resolve แล้วเสมอ ไม่รับ path จากที่อื่น
  — พลาดตรงนี้ทีเดียวคือลบผิดโฟลเดอร์บนเครื่องจริง
- เคลียร์ `_txt_paths` ด้วย เพราะ path ที่จำไว้ชี้ไปยังไฟล์ที่ไม่มีแล้ว

## 1.7 แปลงบรรทัด `.txt` แบบไม่โยน exception (`:166`)

ไฟล์ผลวัดมี 9 ช่อง `"+0005.047,+0005.045,+0000.003,26,07,31,17,28,42"`
บรรทัดที่รูปแบบไม่ตรงคืน `None` เฉยๆ **ไม่ throw** เพราะไฟล์อาจมีบรรทัดแปลกปนมา
ไม่ควรทำให้ทั้งไฟล์ใช้ไม่ได้ · รองรับไฟล์เก่าที่มีแค่ 2 ช่อง (offset = 0)

## 1.8 POST พังแล้วเก็บรูปไว้ (`:459-463`)

```python
except Exception as exc:
    print(f"   ⚠️ POST /api/measurements ไม่สำเร็จ: {exc} — เก็บรูปไว้ไม่ลบ")
    return
```

ตั้งใจไม่ลบ เพื่อให้กู้ด้วยมือได้ถ้าจำเป็น

---

# ส่วนที่ 2 — ที่ยังขาด

## 🔴 2.1 `get_current_session()` ปนกัน 2 ความหมาย (`:255`)

```python
except Exception as exc:
    print(f"⚠️ query /api/session/state ไม่สำเร็จ: {exc}")
return None                      # ← ปนกัน
```

`None` แปลได้ 2 อย่าง — **"ไม่มี session running"** กับ **"ถาม Backend ไม่ได้"**
แต่ด่าน 3 ปฏิบัติกับทั้งคู่เหมือนกันคือ **ทิ้งค่าและรูปทิ้ง**

```
Backend สะดุด 2 วิ (uvicorn --reload / query หนัก / restart)
   → Recieve ถามไม่ได้ → ทิ้งชิ้นงานที่วัดสำเร็จแล้วไปเลย
   → measured_count ไม่ขยับ → Pi รอจน MEASURE_TIMEOUT
```

**ชิ้นงานวัดผ่านแล้วแท้ๆ แต่หายเพราะ Backend แค่กระตุก**

### แก้ยังไง

แยกให้เป็น 3 สถานะแทนที่จะเป็น `session_id` / `None`

```python
session_id          → มี session ให้บันทึก
"no_session"        → ไม่มี session running จริงๆ  → ทิ้งได้ (ถูกต้อง)
"unreachable"       → ถาม Backend ไม่ได้           → เก็บรูปไว้ ไม่ลบ
```

กรณีที่สามให้ทำเหมือนด่าน 4 ตอน POST พัง คือเก็บรูปไว้แล้วปล่อยให้ `clear_temp_dir()`
ตอนจบ session เป็นคนกวาด

## 🟡 2.2 อัปโหลดรูปพัง แต่ลบรูปทิ้งเสมอ (`:315-316`)

```python
finally:
    _remove_quietly(image_path)      # ลบไม่ว่าสำเร็จหรือไม่
```

**ไม่สมมาตรกับด่าน 4** ที่ POST พังแล้วเก็บรูปไว้ให้กู้ได้ — พอมาถึงขั้นอัปโหลดกลับลบทิ้ง

ผลคือได้ measurement ที่ `image_path` เป็น `NULL` และ **รูปหายถาวร กู้ไม่ได้เลย**
ทั้งที่ค่าการวัดบันทึกสำเร็จไปแล้ว

ตัดสินใจให้ตรงกันสักทาง — ผมเอนไปทาง **เก็บไว้ทั้งคู่** แล้วให้ `clear_temp_dir()`
เป็นคนกวาดตอนจบ session (มันทำอยู่แล้ว) จะได้มีโอกาสอัปโหลดซ้ำด้วยมือถ้าจำเป็น

## 🟢 2.3 รอได้ — มีในเอกสารอื่นแล้ว

| เรื่อง | อยู่ที่ |
|---|---|
| ทุก error เป็น `print()` ไม่มี log ไฟล์ | `LOGGING_SPEC.md` |
| `client_uuid = uuid4()` ทุกครั้ง → กันซ้ำไม่ได้จริง | `IMPROVEMENT_PLAN.md` |
| ไม่มี retry เลย — POST พังครั้งเดียวก็ยอมแพ้ | — |
| `create_measurement` ไม่เช็ค state ตอนรับ | `CLAUDE.md` Known Issues |

---

# ⚠ ก่อนใช้งานจริง

```
.env → FORWARD_TO_BACKEND=0     ← ตอนนี้เป็นค่านี้ = "โหมดรับอย่างเดียว"
                                  รับไฟล์มากองไว้ ไม่ POST ไม่ลบ temp
       FORWARD_TO_BACKEND=1     ← ต้องเปลี่ยนเป็นค่านี้
```

เคยหลงคิดว่า **"รูปทะลักเข้ามาไม่หยุด"** เพราะค่านี้มาแล้วรอบหนึ่ง — ที่จริง
สคริปต์ทำงานถูกต้องทุกอย่าง แค่อยู่ในโหมดที่ไม่ลบไฟล์

---

# ลำดับการทำ

| # | งาน | ขนาด |
|---|---|---|
| 1 | `get_current_session()` แยก "ไม่มี session" ออกจาก "ถามไม่ได้" (2.1) | ~10 บรรทัด |
| 2 | อัปโหลดรูปพังแล้วอย่าลบ (2.2) | 1 บรรทัด |
| — | ที่เหลือรอได้ | |

ทั้งสองข้อไม่ขึ้นกับแผนอื่นเลย ทำแยกได้ทันที
